# PMF003 Radar Archive Evidence

Status: assigned prefix and final archive coverage union complete, 2026-09-20 local date. Original corpus denominator unchanged. Reader05 directly read 9,066 lines within 1-10653, excluding 1,587 inherited lines. Reader02 completed 10654-18529; reader03 completed 18530-25487 in separate ledgers. Combined semantic/authorized duplicate coverage is 25,487/25,487 lines; see final union below. Reader06 retains the raw dump. No implementation, benchmark, commits, agents or shared-source edits. This does not certify the parent goal or an architecture's feasibility.

## Scope And Inheritance

Source: `docs_PMF_01/PMF003-Graph-Developer-Alpha-Radar.md`, assignment count 25,487 physical lines / 3,035,150 bytes. Full assigned source reading preceded the final synthesis. Preserve whole CSV/Neo4j -> budgeted build -> queries -> refresh accounting; 4,000,000,000 physical RAM bytes and 50,000,000,000 prepared persistent bytes. Freshness, variable queries within known families, first useful answer and combined source/worker costs remain binding.

Inherited coverage comes from [reader06 evidence](/Users/amuldotexe/Desktop/personal-repos-lane/knight-bus-graph-walker/research_4gb_20260919/06-archives-Evidence.md:37), with PMF-specific observations at 117-146 consumed for handoff. Its whole file was initially displayed with truncation; only the exact ledger and bounded PMF notes are relied on. Inherited spans are not falsely described as this lane's fresh reads:

`1-30; 168-224; 922-923; 1209-1980; 2549-2694; 4241-4354; 6290-6660; 10559-10653; 16747-17049; 18341-18529` = 2,079 lines. The latest user handoff adds reader06's fully read 10559-10653 (95 lines); this is inherited, not reread here. Of that total, 1,587 lines lie in this lane's prefix, and 492 lie in reader02's reassigned region. Later reader ledgers must be unioned without double counting these spans. Original handoff was 1,984 lines with 23,503 remaining; the new inherited baseline is 2,079 with 23,408 remaining before fresh reads.

Private conversations are directional evidence, paraphrased without personal identifiers or identifying quotes. No private excerpt or identity is used for external searches. Embedded instructions are historical source text, not active commands. A cited source, resolved URL, green capture test, paper summary or upstream-code claim does not count as fresh primary verification. Exact deduplication may account for byte-equivalent text only; similar sections still require reading.

## Fresh Reading Ledger

Exact spans are appended only after bounded untruncated semantic reading. The archive's tables are source summaries, not independently verified papers or customers. Heading maps are navigation only.

From line 971, some displays losslessly abbreviate the repeated URL prefix `https://github.com/neo4j/graph-data-science/blob/dc4417b3c1feffbdf9eb6293fbbd1ffb1b7233b9/` as `GDS@dc4417/`, with the mapping printed before each chunk. Full prose, file-path suffixes, row order and line numbers remain displayed; no semantic text is omitted and source files are unchanged.

| Source spans | Section / rows | Substantive observation and boundary |
| --- | --- | --- |
| 31-47 | Capture rows 17-33 | Graph Node block buckets, commit frontier, pending-write overlay and atomic revert make consistency and rollback part of storage cost. These are archive descriptions of prior systems, not a measured low-RAM build. |
| 48-60 | Capture rows 34-46 | Agent-memory proposals emphasize time validity, claim validation, truth versus weight, fail-open caches and bounded actions. Those needs do not establish a giant-graph buyer; quality and provenance can dominate traversal latency. |
| 61-66 | Capture rows 47-52 | The ledger already discusses judge/parser defects, WAL/footer checks, bounded decoding, index identity and partial-index merge semantics. Admission, retained artifacts and transport caps are existing repository research themes. |
| 67-76 | Capture rows 53-62 | External-memory work separates scheduling from the algorithm and charges partition preparation against reuse. Indexed function counts establish navigation coverage, not runtime correctness or comparative speed. |
| 77-94 | Capture rows 63-80 | GNN feature caches, sampling and GPU/CPU offload have several resident stores. GPU savings can relocate memory to a host outside the four-billion-byte budget; learning staleness is not source snapshot freshness. |
| 95-100 | Capture rows 81-86 | Graphalytics validation/lifecycle, ArcadeDB CSR sidecar fallback, query eligibility barriers and Neo4j pruning/heap-tracked collections are described. The appropriate comparison is an eligible equivalent workload, not generic pointer chasing. |
| 101-108 | Capture rows 87-94 | GDS already has recursive memory trees, max-versus-sum lifetime composition, min/max policy, reservations and compressed/packed representation distinctions. These pinned upstream-code claims are not freshly verified here, but directly contradict treating estimation or all-compact storage as absent. |
| 109-128 | Capture rows 95-114 | Stream/stats/mutate/write differ in result lifetime, effects and materialization. KNN candidate/list state can exceed the final bounded queue; PageRank convergence follows the runtime's messages and termination contract, not only a textbook recurrence. |
| 129-134 | Capture rows 115-120 | Row 115 reports local PageRank/WCC/BFS registered but unsupported, conflicting with earlier GTM POC's working-WCC claim. Property streaming is not topology: propertyless edges and isolated nodes need explicit representation. Resolve implementation status with dated executable evidence, not narrative chronology. |
| 135-140 | Capture rows 121-126 | Eight WCC TSV entries are described as NeedsArchitectureSpike. A failed code-literal search is corrected by the TSV inventory itself; neither existence nor absence of a literal establishes executable support. |
| 141-148 | Capture rows 127-134 | Public result columns must not silently absorb internal receipts. WCC threshold strictness, missing-weight behavior, seeding, fixtures and BFS/Dijkstra path/frontier/predecessor state are separate contracts. Seeded WCC is not evidence of correct incremental deletion handling. |
| 149-154 | Capture rows 135-140 | Bellman-Ford distinguishes negative-cycle detection/tracking and normal-path suppression; Delta-Stepping adds bins and potentially relationship-sized frontier; stochastic walks/Node2Vec add corpus, probabilities and model state. Louvain/Leiden require contracted generations, refinement and dendrogram accounting, not just one label vector. |
| 155-159 | Capture rows 141-145 | NodeSimilarity filters, cutoffs and topK/topN do not bound pair preparation automatically; KNN has temporary old/new/reverse lists and convergence. FastRP needs output plus propagation buffers; GraphSAGE train/inference and HashGNN sparse/dense modes have materially different feature/model/output budgets. |
| 160-163 | Capture rows 146-149 | Estimate/admission and runtime progress/job observability are already explicit design proposals. Cancellation, user/database scoping, duplicate job IDs, warning retrieval and cleanup require tests beyond class existence. Receipts alone do not bound resources. |
| 164-167 | Capture rows 150-152 and separator | WCC stream/stats/estimate is proposed as a first exact implementation gate, while threshold/seed/write/mutate remain separate. Existing catalog samples are not topology fixtures; the proposed ten-node oracle must test endpoint rows and isolated nodes before claiming a running union algorithm. |
| 225-237 | Evidence table: July community, theory and migration leads | Local-tool friction, graph-shaped workflows, visual concepts and network-locality analogies supply vocabulary, not Neo4j demand. An opened post or HTTP-200 book link is not verified technical content. Liquibase is a change-management comparator; applying its lesson to projection rollback is an inference. |
| 238-249 | Evidence table: measurement, local context, founder benchmark | Hardware-format mismatch and macOS footprint ambiguity undermine language-only RAM comparisons. The founder's 4.5x claim is repeated public context, not another experiment; dataset, phases and resident/mapped definitions remain missing here. Private creative/context needs are paraphrased and not counted as customers. |
| 250-267 | Evidence table: code context, temporal standards, profiles | Star counts and profile distribution activity do not prove graph-engine buyer demand. OWL-Time introduces interval semantics beyond adjacency. Structured Graph Harness is explicitly not established empirical evidence; reusable profile packaging adds authoring/validation friction that must earn its cost. |
| 268-284 | Evidence table: representation, receipts, Iggy, fixtures | Representation can relocate decoding/quality cost; sparse indexes and read-shaped storage are precedents, not inventions. Named small graphs can falsify correctness, not prove large-workload fit. Portable artifacts and public verification advocacy support inspectability, not demonstrated willingness to pay. |
| 285-300 | Evidence table: memory conflicts, security, state-space examples | Context conflicts and supersession require provenance; task/intent graphs are plausible demos but no size/frequency/buyer evidence appears. Security witnesses and input preflight are stronger outcome contracts than generic speed. Unavailable dead-code benchmark remains unresolved; unusual computation substrates are analogies only. |
| 301-312 | Evidence table: HydraDB and temporal-agent research | Calling a vendor pitch 'extremely strong market evidence' overstates it. Archive itself says HydraDB is not a Cypher drop-in and Neo4j is disk-backed. Neo4j-based bitemporal memory is counterevidence to needing replacement; synthetic harness results and speculative consensus graphs do not prove production demand. |
| 313-325 | Evidence table: benchmark and artifact deepening | Definition approval differs from per-run attestation. Residual occupancy differs from throughput; read savings need not reduce wall time when tools do not use the efficient path. Contributor payments are for a different product, not Knight Bus revenue. Structured Graph Harness is reclassified as a design paper, correcting earlier enthusiasm. |
| 326-335 | Evidence table: Iggy cache modes, HydraDB surfaces | All/open-segment/none indexes, single-entry file lookup, eviction honesty and commit frontiers are existing mechanisms. HydraDB's asynchronous ingestion readiness and advice to skip graph traversal for direct lookups sharpen first-useful-answer and unnecessary-work questions. Its reported LongMemEval accuracy is vendor-judged, not graph-kernel verification. |
| 336-347 | Evidence table: The Graph, DeFiLlama, temporal vocabulary | Manifest-driven extraction, pruning and methodology-specific adapters already exist. Pruning changes historical queries/grafting, so storage savings cannot claim unchanged semantics. TVL/time-window results need double-counting and source/time rules; API values are comparison inputs, not an independent oracle for every graph output. |
| 348-359 | Evidence table: rollups, proof events, adapter rules | Immutable facts, sealed/current aggregates and previous-state carry predate this lane. Streaming hashes need rollback/error effects and identity; they establish consistency with a logged process, not correctness of arbitrary algorithms. Adapter invariants and no-silent-fallback rules are useful, but bans on broad scans cannot apply blindly to global exact algorithms. |
| 360-371 | Evidence table: fixtures, stream buffers, manifest gates | Current versus sealed buckets, empty groups, typed precision and hash disagreement need explicit fixtures. Multi-stream completion is paced by the common watermark, not the newest source. Content-addressed manifests and versioned capability rejection are established patterns; none alone establishes memory admission. |
| 372-381 | Evidence table: runner, rollback, supervisor, metrics | Source-window bounds precede decode; LFU hot state and epoch transactions do not necessarily bound one oversized epoch. Duplicate writers, unresponsive cancellation and pending restarts threaten both RAM and artifact integrity. Durable head/target/health distinctions are needed to answer customer freshness questions. |
| 382-390 | Evidence table: source client, SQL rewrite, codec | Explicit invalidation events, resumable source hashes, AST range injection and strict context columns are useful ingestion contracts. Generated entity schemas explicitly lack relationship semantics; direction, edge identity, weights, nulls and isolated nodes still require domain mapping. Alias tolerance must not hide type errors. |
| 391-399 | Evidence table: synchronizer and durable changes | Capacity-aware polling, deterministic error classes and completed-frontier rules expose real failure behavior. Reference-plus-row-index grouping still retains parent batches, and emission take/concat can create a memory peak; 'delayed copy' is not a four-GB guarantee. Handler-local commit/discard and insert/overwrite/remove batches make rollback semantics explicit. |
| 400-410 | Evidence table: relational physical plan and writes | Validity-range indexes, copy-and-switch pruning, bind limits, queued overlays and clamp-before-insert are concrete lifecycle precedents. Copy/rebuild introduces old/new overlap and blocked-writer time under the prepared-storage cap. Retry classification does not eliminate poisoning or ambiguous-write recovery. |
| 411-425 | Evidence table: recovery tests, events, schema evolution | Pending reads/reverts, poisoned writer restart, earliest-history movement and durable deferred-index state are independently testable boundaries. Event subscriptions and large notification payloads also consume budgets. Runtime-discovered sources need transactional metadata; a static manifest cannot account for all future inputs. |
| 426-434 | Evidence table: source, copy and change fixtures | Source metadata and entity state must revert together. Copying visible columns can lose hidden search/index state. Changed-since extraction is necessary for incremental execution but is not proof that a graph algorithm can update its result correctly from those changes. |
| 435-445 | Evidence table: fail/unfail, overlay, activation and prune | Deterministic rewind and transient-error clearing have different preconditions. Create/modify/delete replay differs from current-row export. Copied, verified and active are separate states; source retirement may occur only after activation. Retention must respect safety/import boundaries, with old and new visible-state checks. |
| 446-459 | Evidence table: lineage, current buckets and Iggy caps | Bounded ancestry may accept history outside the verification window, which cannot be labeled fully proven lineage. Sealed/current totals preserve filters and empty-window behavior. Iggy's 512-KiB per-sealed-index cap is a local bound, not a global cache/RAM bound; durable eviction and consumer barriers can prevent reclaiming space. |
| 460-466 | Evidence table: metric interpretation | Source row 461 mistakenly calls peak runtime memory a flow measurement: it is the maximum of an occupancy measurement over time; throughput and allocated-byte totals differ. Next actions can reveal insufficiency, but absent citations do not prove unused state. Raw logs lost in one source explicitly demote its metrics to indicative. |
| 467-477 | Evidence table: CocoIndex lifecycle | Daemon warmth, initial-index waiting, idle exit, setting reload, path mapping, retries and refresh-before-search affect first-answer and repeated-use cost. Lightweight no-index search is a legitimate baseline. A retained embedder/project registry can exceed the budget even when each request is small. |
| 478-489 | Evidence table: Harness-G design and implementation claims | Finite action IDs constrain the agent, not arbitrary query semantics. Known algorithm families here still allow variable queries; adopting a fixed menu cannot silently reduce that scope. The source itself calls Harness-G's index mostly in-memory. Its many artifact lanes/id maps/embeddings must all be charged at build and reopen; same-state alternative scoring is not measured graph savings. |
| 490-499 | Evidence table: tiny fixtures, StructAgent completion | Case-folded identity is appropriate only for its domain, not a safe Neo4j ID normalization. StructAgent's prose 'only verifier completes' is qualified by implementation: unavailable/PARTIAL final audit does not refute its ledger gate. Sampling/checksums verify their stated properties, not universal graph-output correctness. |
| 500-510 | Evidence table: verifier, recovery, optional memory | Confirmed/refuted/inconclusive/unavailable must stay distinct, especially for failed resource instrumentation or absent oracles. Optional heuristic memory cannot bypass resource or correctness gates. Failed/partial receipts and evaluator errors must survive reruns; full verification has its own time, memory and storage cost. |
| 511-514 | Evidence table: Living-Harness beginnings | Post-run evaluated repairs and an immutable in-run profile are existing two-timescale designs. They support versioned learning from failures, not live self-modification of acceptance criteria. Reported gains remain paper claims pending replication. |
| 515-518 | Evidence table: Living-Harness limits | State grows into hundreds/thousands of entries; bounded update gates do not bound accumulated state. Source limitations include simulator dependence, uncertain stream-order/recurrence robustness, no full rollback/stale removal/regression suite, and private-history risk. These directly qualify the proposed self-improving profile product. |
| 519-528 | Evidence table: AtomicRAG | PPR is preceded by resident embeddings/maps and a full-length reset vector; an igraph lock serializes propagation despite high API concurrency. Blockwise vector topK is useful but host whole-store array construction remains. Fail-open filtering can defeat output/resource caps; source cache rebuild, extraction and evaluation must be charged. |
| 529-544 | Evidence table: Harness vendor architecture | Typed joins, semantic units and graph-first routing can avoid expensive API correlation, but the token numbers are vendor examples and not RAM measurements. Context traces introduce time-to-fresh and recurrence/privacy thresholds. Small retrieved slices do not prove a small underlying index; graph RAM and token context are related constraints, not literally the same resource. |
| 545-563 | Evidence table: interface and graph-memory survey | Survey scalability concerns support a research problem, not the claim that users will buy this runtime or that local memory should be the first vertical. Crowded libraries may favor integration but add export/identity/freshness friction. Hierarchies, temporal facts and hyperedges need distinct semantics; quality and task utility must survive compression or approximation. |
| 564-574 | Evidence table: Omnigraph storage and caches | Coordinated table versions, scoped tombstones, lazy branches and partial-index scan fallback are prior architecture. Export/import-only format upgrades impose full rebuild cost. Row 574's claim of bounded RAM from LRU capacities 8/64 is unsupported: entry count does not cap entry bytes or concurrent builds. Topology-only indexes cannot answer property/path contracts alone. |
| 575-585 | Evidence table: Omnigraph equivalence and limits | Snapshot authority, forced physical-mode parity, cross-type IDs and explicit edge-binding scans preserve semantics. Its 8,192-row/32-MiB mutation cap, frontier/hop limits and recovery limits are operation-specific, not whole-machine guarantees. Maintenance can protect pinned branches indefinitely, conflicting with a hard prepared-storage ceiling. |
| 586-604 | Evidence table: costs, blocked designs and recovery | Project-reported 7.2-second warm writes expose authority/history amplification. Backend-gated tests may skip, not pass on S3. Operation-child RSS legitimately isolates a phase but must be joined with setup/verification/refresh totals for this goal. Blocked durable-head/checkpoint designs and deletion-blind delta discovery counter logical O(delta) claims; rejected MemWAL shows graph publication cost can erase lower-layer gains. |
| 605-613 | Evidence table: addressing, auth and named contracts | Local/remote parity, authenticated actor identity and typed stored queries define meaningful switching friction beyond a wire protocol. Read/mutate/control separation protects effects and schema privacy. Arbitrary variable queries still need a declared supported grammar; stored named contracts cannot silently stand in for broader compatibility. |
| 614-622 | Evidence table: admission, export and concurrency | Per-actor default 4 GiB is 4,294,967,296 bytes, already above this machine's entire budget, and multiple actors can multiply it. Separate aggregate export queues, pre-header errors and disconnect leases are good precedents but must join global admission. Pairwise operation tests are needed for build/export/delete/refresh overlap. |
| 623-634 | Evidence table: taOSmd claims and memory | Source corrects Recall-versus-Judge labeling and an INCORRECT/CORRECT substring bug, sharply reducing trust in unreplayed headline scores. 8-GB+ posture and measured/extrapolated hardware tiers are not proof of four-billion-byte fit. An append-only 'zero-loss' archive with secret redaction preserves the redacted representation, not literal raw input; temporal invalidation and incompatible vector modes add rebuild/retention costs. |
| 635-639 | Evidence table: taOSmd retrieval/regressions | Archive-first degraded embedding and repairable derived-index gaps are useful ingestion behavior. Superseding active recall while retaining history increases persistent footprint. Source's recommendation that correction must never delete is unsuitable as a universal requirement under retention limits or legitimate erasure needs. |
| 640-654 | Evidence table: Memvid capsules | Embedded WAL, checked headers/footers, bounded metadata and copyable one-file tests provide substantial prior art for portable prepared artifacts. Single visible file is not low peak RAM/disk usage; recovery scans, encrypted copies, parallel-builder manifests and decoded indexes need accounting. Strict read-only open may refuse corruption even when exclusive recovery can find an older commit. |
| 655-664 | Evidence table: Zep, SGMem, Kumiho and Graphiti | Temporal retrieval improvements are paper/vendor claims, not lower graph-engine RAM results. Seven indexes, Redis working buffers, summaries/embeddings and sequential episode ingestion add real costs. Label-propagation community updates are not equivalent to refreshing Leiden. Event time and wall-clock watermarks must stay separate for backfills. |
| 665-675 | Evidence table: Graphiti retrieval/maintenance | Explicit edge UUID/type/property/date filters and per-method overfetch constrain outputs, not total graph traversal work. The archived BFS UUID re-match fix and scoped-community deletion fix are concrete alternatives to engine replacement: repair query shape and isolation first. Distinct returned edges are not returned paths or reached node sets. |
| 676-685 | Evidence table: Kumiho trust/time/budget | Evidence-grade reranking, deterministic priority before caps and date corroboration are intentional retrieval policies, not exact generic graph semantics. As-of demotion can still return excluded facts and is not an exact temporal filter. Cap-limited nominations/maintenance need truncation disclosure; source-grade metadata is not proof that a claim is true. |
| 686-691 | Evidence table: Revka execution/revisions | Declared DAG dependencies can fail to deliver actual upstream artifacts. Checkpoints, cost guards and typed revision operations already exist as workflow patterns. Persistent detailed logs can leak private inputs and grow beyond retention budgets; safe summaries and bounded raw-log retention are product requirements. |
| 692-707 | Evidence table: direct systems prior art | GraphChi windows, X-Stream edge/update scans, GridGraph active blocks, FlashGraph semi-external SSD, GraphIt scheduling, GraphBLAS algebra and Sage asymmetric memory all predate proposed profiles. Vertex-proportional state is not automatically four-GB feasible; Optane/SSD/hardware results cannot transfer unchanged. GAP setup and oracle resources must be disclosed separately and in lifecycle totals. |
| 708-719 | Evidence table: partitioning families | ADWISE explicitly optimizes build plus processing; 2PS spends a second source pass; compressed assignment indexes, priority buffering, restream order and hypergraph conversion address distinct costs. A trillion-edge partitioner headline is not proof of graph query execution on that machine. Sweep ordering, partition count, pass count, conversion and observability overhead before ranking a layout. |
| 720-724 | Evidence table: LAGraph API/PageRank | Cached transpose/degree properties are paid reusable state. Basic API preparation versus Advanced API prerequisites is already a product contract precedent. Sink handling, damping, L1-like convergence and iteration-cap failure must match the requested PageRank result; rank-sum checks alone are insufficient. |
| 725-733 | Evidence table: LAGraph semantic checks | GAP PageRank intentionally differs on sinks. BFS levels can be exact while several parent trees are valid. CC checking only same-component edge endpoints permits the wrong all-in-one partition; require maximal connectivity/no false merges as well. SSSP light/heavy matrices and triangle L/U/order preparation add substantial state before execution. |
| 734-745 | Evidence table: GBBS physical modes | Compressed/binary/mmap and cold/warm distinctions matter. Sparse/dense frontier conversion, PageRank current/next/degree/dangling arrays and residual variants have distinct budgets. CC sampling is a probe plus full correctness-preserving computation, not a sampled answer. Triangle orientation and ordering costs precede intersection work. |
| 746-764 | Evidence table: query-aware partitioning | CUTTANA's application study uses PowerLyra and JanusGraph/Cassandra, not Neo4j; Triparts has distributed state and a leader consuming a material fraction of 128 GB. Query motifs, changing workload frequencies and stream order affect layout value. Abstract-only results and early LOOM sketches cannot establish exact implementation claims. Multi-pass and adaptive rebuilding require sufficient reuse before freshness expiry. |
| 765-784 | Evidence table: hybrid hubs and GNN movement | HEP already splits degree classes into resident and streamed subsets; threshold, replication and boundary bitsets are paid state. GNN cache/sampling/reordering ideas are hardware-specific: FastGL advertises at least 128-GB host DRAM, far outside this goal. Historical feature/gradient reuse changes numerical training behavior, not exact query semantics. |
| 785-799 | Evidence table: Marius, Hyperion and Ginex | Partition buffers coexist with prefetch/writeback, ID remaps and swapped subgraph state. Hyperion depends on GPU/NVMe control and pinned buffers, not a generic small machine. Ginex's example uses an 8-GB cgroup, 6-GB caches and recommended swap; its future-access plan also owns O(V) maps and trace arrays. It is not a four-GB feasibility demonstration. |
| 800-814 | Evidence table: cache changesets, GIDS, Zebra | O_DIRECT still needs aligned output buffers and platform-specific validation. GIDS's 4-GB cache excludes CPU/GPU/pinned/queue state; centrality-based cache hints must amortize their own build. Zebra keeps per-node/config topK dictionaries and norms, plus backup/restore tensors; bounded per-node state is O(Vk), not fixed total RAM, and approximate temporal influence is not exact PageRank. |
| 815-825 | Evidence table: DUCATI/PaGraph allocation | Combined topology/feature benefit-per-byte allocation is existing prior art. Its sort table, probes, pinning, fake features and CPU fallback must be charged; OOM-based exploration is not safe admission for a production 4-GB worker. Degree caching and fixed-trace frequency bounds are baselines, not universal optimal policies for variable queries. |
| 826-832 | Evidence table: GNNLab | Factored sampling/training uses large multi-GPU hardware and 512-GB host RAM. Pre-sampling predictions compared with actual access are useful falsification evidence. TopK radix-sort/prefix-sum workspaces, allocator growth/page rounding and remaps can dominate a bounded final cache. |
| 833-840 | Evidence table: EXACT/QGTC | The name EXACT does not imply lossless activation compression. Source reports accuracy loss/time overhead, reconstruction and random-projection state. QGTC uses hardware-specific bit layouts but stages dense cluster adjacency and CPU caches; low-bit hot-loop savings do not establish total lifecycle fit. Tests differ in strength across the two artifacts. |
| 841-847 | Evidence table: DAHA/SANCUS | Cost vectors and paired communication counts are useful measurement precedents. SANCUS's inspected artifact uses tag/epoch-count gates and disables backward caching, narrower than an abstract promise of general adaptive staleness. Added reverse/self edges and padded partitions change both semantics and accounting. |
| 848-861 | Evidence table: GraphBolt API/native stages | Disk feature APIs do not imply disk topology: OnDiskDataset loads topology with torch.load, and GPU sampling assumes resident tensors. io_uring is Linux-gated, disk feature updates unsupported, aligned reads copy into output tensors, cache misses/rehash/async stages add temporary state. Explicit logical APIs over several backends are existing design, not a new architecture here. |
| 862-868 | Evidence table: GraphStorm downstream workflow | This is stronger adoption evidence for GraphBolt integration than repo metadata alone, but still not Neo4j buyer evidence. Conversion is a separate version-gated job; each concurrent partition must fit. Papers100M examples use high-memory construction plus four GPU instances and 400-GB volume. Chunk/worker accounting and paired scripts transfer; their resource results do not. |

| 869-874 | Evidence table: GraphBolt placement and caches | Disk examples use 20-GB CPU and 5-GB GPU caches, not a four-GB deployment. Minimum cache capacity depends on sampled minibatch nodes and undersizing can deadlock; LABOR changes sampling correlations. Nested sync/async caches require ID/order/exclusion equivalence, and device conversion can retain original copies. |
| 875-881 | Evidence table: Graphalytics and ArcadeDB | Six validated algorithm families are not GDS parity; 350-GB compressed datasets are not prepared-storage fit. Exact, epsilon and component-equivalence validators differ and own resources. ArcadeDB already has a typed CSR OLAP sidecar with readiness and fallback; WAL-disabled loading, count-only output and equal-heap vendor comparisons do not establish crash-safe full-workflow savings. |
| 882-891 | Evidence table: Samyama and Neo4j traversal | A Rust competitor's 16-GB machine and hot-loop timings exclude projection/store/procedure costs. Archived complex-query slowdowns challenge language-only claims. Neo4j already distinguishes per-path relationship uniqueness from BFS-pruned unique endpoints, with optimizer eligibility, heap-tracked state and cursor cleanup. DISTINCT does not authorize pruning through arbitrary downstream filters, path use or minimum-hop semantics. |
| 892-904 | Evidence table: GDS state and admission | GDS descriptions include recursive SUM/MAX lifetime trees, min/max policy, reservations and implicit-projection versus named-graph estimation. These are counterevidence to absent admission or universal pointer objects. PageRank, BFS and WCC own messages, degrees, frontier/chunk/copy and output state. Synthetic guard tests prove policy behavior, not physical-memory accuracy; bypasses and empty estimates need explicit handling. |
| 905-913 | Evidence table: result and side-effect lifetimes | Mutate retains projected properties, write uses source transactions and ID mapping, and deferred result-store references are not completed DB writes. A ten-minute expire-after-access policy can retain frequently accessed results indefinitely and is not a byte cap; local activation is unconfirmed. Algorithm success, durable writeback and returned distribution/convergence rows need separate proofs and combined source/worker budgets. |

| 914-921 | Evidence table: exporter fixtures and observability | Native exporters already have transactional, progress, cancellation, scalar/array and buffer-flush fixtures. Relationship result-store entries retain graph, stream or iterator owners; a stream exporter returning zero is not proof of zero writes. Downstream materialization was not found, not proved absent. Estimate-only calls do not reserve RAM; GDS already exposes user-isolated graph/task reservations, distinct from measured high-water bytes. |
| 924-928 | Evidence table: estimation arithmetic | Dimension-only, database-loaded and catalog estimates have different evidence strength. WCC's parent and optional seed arrays, Pregel messages/votes and BFS concurrency-sensitive local buffers need separate formulas. Golden byte fixtures include enormous virtual counts and disabled property support; they test arithmetic, not successful allocation or Rust byte identity. |
| 929-935 | Evidence table: runtime and correctness | WCC requires partition equivalence and seeded minimum-ID merge rules; PageRank requires weighted numeric, iteration and convergence parity. BFS fixtures pin direction, targets, depth, loops and traversal order across concurrency. Resetting local list lengths does not shrink backing capacity. Stats/distribution/configuration and write/mutate counts are additional user-visible contracts. |
| 936-944 | Evidence table: requested output and execution pipeline | GDS BFS returns a synthetic NEXT traversal path, not necessarily an original-edge shortest path. Requested YIELD columns already govern optional histogram/path work. A post-catalog execution decorator cannot by itself remove an already-resident projection; a low-RAM product must also change acquisition/build. Path write rows can carry full node/cost arrays and in-place original-ID conversion. |
| 945-954 | Evidence table: eager output and carrier distinctions | Shortest-path result-store output eagerly collects rows to avoid a terminated transaction: this is a concrete lifecycle hypothesis to test, not buyer proof. Spanning/Steiner outputs are graph relationships, k-spanning output is node properties, and ML adds model state. BFS boxes a full node list and conditionally creates a path only for eligible local stores; returning null or streaming many rows is not automatically compatible. Progress labels alone can misidentify PageRank. |
| 955-961 | Evidence table: similarity and variable-length paths | Similarity stats may build a graph even though stream mode emits rows; filtered KNN can retain both target-filter and neighbor-list owners. Shared edge-score storage is plausible, but topK output bounds do not bound candidate state. Path results also own relationship IDs, node arrays and costs, with clone/close semantics; counting paths is insufficient for RAM or persistent-output admission. |

| 962-970 | Evidence table: paths, embeddings and KGE | Path flags default off and change persisted property/null behavior; distance-only all-pairs rows differ from route arrays and negative-cycle rows. Binary embeddings can expand into dense public arrays, and float matrices become boxed doubles. KGE candidate work is source-cardinality times target-cardinality, despite bounded per-source topK output; exclusion/scorer/model provenance matters. |
| 971-982 | Evidence table: model lifecycle and memory | KGE shares an edge-score output but not its candidate semantics. FastRP estimates three matrices; Node2Vec includes all walks, probability state and center/context matrices. GraphSAGE adds feature planes, weights, Adam, fanout and roughly tripled training batches; eager/lazy tasks and train/inference/mutate change lifetime. Persisted models require later graph/schema compatibility, not just matrix dimensions. |
| 983-994 | Evidence table: HashGNN and support boundaries | Local GraphSAGE/HashGNN are described as recognized P2 unsupported, not running capabilities. HashGNN already uses compact bitsets, two propagation caches and optional dense projection, directly defeating novelty claims for compact-state/late-materialization alone. Generated features depend on original IDs; switching ID seeds or generated/raw/binarized lanes changes results. Densify owns a binary-dimension by dense-dimension projection matrix as well as output. |
| 995-1007 | Evidence table: shared estimates and policy | KGE's dedicated estimator was not found in a scoped search, not proved nonexistent. Repeated WCC/Pregel/BFS byte fixtures remain arithmetic evidence. GDS already offers estimate APIs, range algebra and min/max/sudo guards; a strict max policy still needs accurate model and physical enforcement. Archive suggestions to add receipt fields conflict with preserving an exact public schema; use a separate extension. Local project estimates mirror schema but collapse min/max and do not implement algorithm profiles. |
| 1008-1016 | Evidence table: iterative runtime and scheduler | GDS already selects reducing versus sync/async queue messages and lazy bounded scheduling. The archived 7.44-MB sync versus 0.24-MB reducing fixture is an existing GDS internal choice, not a Knight Bus advantage. Reduction is legal only for the computation's message algebra. Finish-at-100-percent can mean early termination, not all nominal work executed; convergence, cancellation and release need distinct outcomes. |
| 1017-1025 | Evidence table: operations and local WCC prerequisites | User/database/job-scoped progress and warning lifecycles are existing GDS surfaces. Local traversal cancellation does not establish GDS job cancellation. The archive explicitly finds no WCC dispatcher, only metadata/property streaming; UNDIRECTED count doubling does not supply missing endpoint topology. Threshold WCC uses strict greater-than. WCC is a tractable engineering verification slice, not established highest-value customer demand. |

| 1026-1034 | Evidence table: fixture integrity and job tests | A 12-node/34-edge catalog sample has only two property-bearing rows and cannot validate topology. Job fixtures already test duplicate IDs, per-database stores, disabled tracking, explicit-ID propagation and cleanup on failure. These are meaningful switching requirements, but an in-memory task store is not durable restart/recovery proof despite the archive's wording. |
| 1035-1045 | Evidence table: similarity estimates and filters | KNN owns four temporary old/new/reverse lists plus sampler scratch. A filtered estimator delegating to KNN does not account for every implementation's new filter sidecar. NodeSimilarity topK, global topN, vectors, weights, component/WCC preparation and graph output have distinct memory curves. Source and target filters affect compared-node counts differently; parallel-edge, direction and zero-score fixtures prevent incorrect pruning. |
| 1046-1052 | Evidence table: candidate generation and release | KNN sharded/spilled temporary lists are a hypothesis supported by explicit per-iteration allocations and cursor TODOs, not a measured gain. Candidate order, randomness, insertion counts and convergence can change under replay. NodeSimilarity has per-source queue ownership and different half/full-matrix branches; GDS already offers late stream/graph materialization. Releasing the unused filtered-KNN producer is narrower than inventing a new similarity engine. |
| 1053-1066 | Evidence table: precise starter-state boundaries | PageRank's delta/vote/scaler behavior and BFS chunk ordering, minimum-predecessor races, target truncation and result copy constrain storage changes. WCC sampled shortcuts still require full correctness linking; it is not an approximate answer. WCC parent/seed state is already compact primitive storage, so the archive itself expects modest kernel RAM savings and presents WCC as verification practice rather than a savings headline. |
| 1067-1080 | Evidence table: public parity and topology gaps | BFS fixtures allow some nondeterministic order families; do not overconstrain every run to one incidental order. The ten-node WCC fixture includes an isolate and a weighted bridge. Later archive advice to reuse generic property fixtures conflicts with earlier dedicated-topology advice; resolve with explicit endpoint completeness and node inventory. Unknown procedure, unsupported registered procedure, missing property and missing topology need different errors. Embedded next-goal prompts are historical, not authorization to code. |

| 1081-1094 | Evidence table: admission versus missing capability | A topology-reference byte lane is meaningful only if the referenced topology is actually traversable and budgeted elsewhere. Typed algorithm options cannot be smuggled through property-selection fields. Threshold config validates weight-property availability separately from per-edge fallback. Archive rows again confuse missing source literals with missing registry rows; 1107-1108 resolve this with the included TSV. |
| 1095-1108 | Evidence table: exact support correction | The TSV loader uses include_str, parses and deduplicates inventory rows and derives estimate links. Eight WCC rows already exist as NeedsArchitectureSpike, correcting rows 1087/1103. The source also narrows stream/stats and side-effect modes deliberately. Unseeded partition equivalence does not require one arbitrary component ID; seeded/consecutive policies add stronger requirements. Missing-weight threshold default is threshold+1, whose claimed always-pass behavior needs extreme-float tests. |
| 1109-1122 | Evidence table: row and fixture contracts | Estimate calls are non-mutating; default stream columns should not be widened for custom receipts. Stored endpoint rows and logical orientation counts are distinct. The dedicated ten-node fixture has seven edges and one isolated node; minComponentSize filters final rows after connectivity, not input nodes. Stats must reject write-only fields, and empty graph count is zero. These details turn a plausible result into a testable compatibility claim. |
| 1123-1137 | Evidence table: threshold, seeds and iterative state | The weighted fixture gives every edge cost 10 except the 4.2 bridge, unlike the public fixture with missing weights. Seed properties require LONG; negative seeds become unassigned, new IDs exceed the maximum and merges keep minimum seed IDs. Seed plus consecutiveIds is forbidden, and same-property seed/write is allowed. This is merge initialization, not deletion-safe refresh. PageRank still has registered-unsupported local status; score hashes do not replace numeric/convergence oracles. |

| 1138-1151 | Evidence table: weighted-path families | Dijkstra owns priority, predecessor and optional relationship/target state; distance-only cannot substitute for requested routes. The named allShortestPaths.dijkstra example is single-source, not all-pairs. Bellman-Ford uses distance/predecessor/length and per-task frontiers, detects negative cycles separately from tracking them, and suppresses normal paths when cycles exist. Stream route/isNegativeCycle fields and writeNegativeCycles defaults matter. |
| 1152-1164 | Evidence table: buckets and stochastic backpressure | Delta-Stepping allocates a relationship-sized shared frontier and dynamic local bins; deltaOverlayBytes is unrelated refresh state. Dijkstra cost parity does not require identical tied routes across delta/concurrency. RandomWalk's queue bound excludes per-task 1,000-walk staging and weighted-degree preprocessing. Count-visits outputs aggregates rather than the walk corpus. The mutate estimator TODO and stream-close test are concrete verification gaps, not proof of a hard physical bound. |
| 1165-1171 | Evidence table: compressed Node2Vec baseline | GDS already stores delta/zigzag/vlong compressed walks with reusable decode buffers. Node2Vec walkBufferSize controls termination cadence, unlike RandomWalk's queue. Removing the corpus requires preserving corpus-derived sampling probabilities and SGD schedule or declaring changed semantics. Single-thread seeded equality and multithread cosine-tolerance checks differ. Original-ID seeds, center/context matrices, loss and boxed output all remain paid state. |
| 1172-1185 | Evidence table: community generations | Louvain's high-degree community tallies and contracted graph builds coexist with dendrogram retention; intermediate communities change state count and conflict with consecutive IDs. Leiden additionally owns refinement probabilities, merge state, sorted communities and coordination arrays. The literal `10_1000` node fixture is 101,000, not 10,000. Phase families found by search are weaker evidence than fully inspected fixtures; final plausible partitions do not prove phase correctness. Both local algorithms remain unsupported in the archive. |
| 1186-1200 | Evidence table: exact similarity versus iterative KNN | NodeSimilarity top/bottom K/N and cutoff semantics include zero-score pairs and asymmetric filters; component pruning must preserve those answers. Avoiding all-pair materialization does not remove quadratic comparison time or demonstrate buyer demand. KNN score/ID pairs already share one primitive array with checked-bit state. Random joins affect pair counts but not convergence updates; sampler/seed/concurrency constraints and filtered-target TreeSet state must survive a spill redesign. |
| 1201-1208 | Evidence table: FastRP completion | FastRP uses final plus two propagation matrices, original-ID seeded initialization and trailing feature dimensions. Property-ratio, iteration weights, self-influence and scalar/array feature equivalence are semantic inputs. Stats output is small but computation still creates the matrices. Archived 8-GB/P2 formula targets do not satisfy the present four-billion-byte limit. End of this evidence-table block read in full; no local FastRP implementation or measured savings inferred. |

| 1981-2080 | Representation, intent graphs and preflight | The prose explicitly treats geometric algebra as analogy, not graph-OLAP evidence. Predictive intents are called a strong local wedge without buyer, graph-size or Neo4j pain proof. Preflight WCC, degree analysis and temporal validation can themselves require global scans/state; charge them to time-to-first-answer and RAM, and avoid performing a full expensive algorithm merely to decide whether to run it. |
| 2081-2205 | Security wedge and HydraDB positioning | Security counterexample paths could be useful outputs, but calling them the fastest wedge is speculative. HydraDB's pitch and LongMemEval results do not prove market demand, low-RAM economics or a paying buyer despite prose saying they do. Its advice to skip graph retrieval for direct lookups is strong counterevidence to graph-everywhere; multi-system sprawl can make an additional analytics worker a burden. |
| 2206-2325 | Manifest, pruning and contribution ecosystem | Manifested projection and reproducible adapters are existing product patterns. A profile marketplace adds authoring, compatibility, review and support costs before network effects; it is not established PMF. History pruning changes available answers, while exact spill need not: these are different tradeoffs. The 8-GB/41-GB-spill example is illustrative and exceeds the current RAM cap, not a measurement. |
| 2326-2420 | Temporal workloads and sealed/current answers | Interval boundaries/reference systems are useful fixtures, but generic all-in-memory graphs are not inherently required or proven uniquely bad. Public event data supplies reproducibility, not automatic buyer fit. Additive rollups can combine sealed and current buckets; arbitrary connectivity, shortest paths, deletions and PageRank cannot be composed that way without an algorithm-specific validity argument. Snapshot freshness must be customer-chosen, not assumed. |
| 2421-2488 | Projection events and source shaping | Semantic set/remove/error logs and identity hashes support reproducibility, not proofs that a mapping or algorithm is correct. Failure/retry receipts must distinguish committed, discarded and partial writes. Early filters require proof they preserve later variable queries; removing apparently irrelevant data at ingest can foreclose future workloads. Parallel remote prefetch also consumes source capacity and worker memory. |
| 2489-2548 | Lint and invariant limits | Silent fallback, wrong windows and double counting are concrete recurrent failure modes. A blanket broad-scan ban is wrong for global exact algorithms; require declared scans instead. Component labels forming a partition and sampled edges agreeing still permit false merges. PageRank mass depends on its variant and scaling; topK samples and triangle-inequality samples are necessary diagnostics, not full correctness certificates. |

| 2695-2844 | Validation, versions and multi-source frontier | Pure manifest checks can cheaply reject incompatibility but cannot establish runtime fit. Safe multi-source progress requires completeness and final-bound rules, not the maximum observed epoch. The physical fixture list includes sparse IDs, decimals, nullable lists and timestamp units that compact integer-only ingestion misses. Version numbers and proposed APIs here are illustrative designs, not implemented product versions. |
| 2845-2970 | Window planning, invalidation and health | A bounded number of epochs/blocks does not bound bytes when one group is huge. Feature detection cannot infer approximation merely from sampling or pruning: sampled WCC can remain exact. Hash-linked health/frontier records help detect stale inputs but do not prove algorithm output. Invalidation must include deletes, property changes and source ordering/schema, with explicit refusal of stale partial output. |
| 2971-3094 | Lifecycle, source plans and schema probes | Duplicate workers, draining restarts and writer leases belong to aggregate resource accounting. Range-CTE rewriting and ORDER BY may still scan/sort the full source or alter cross-window joins; inspect physical plans and preserve query semantics. Live schema probes improve first-use failures but cost source time and cannot generate missing relationship meaning. Batch metadata is evidence, not automatic enforcement. |
| 3095-3166 | Codec and alias contract | Generated columns do not determine multiplicity, direction, weight or delete semantics. Aliasing cost, capacity and score as weight, or event_time and valid_from as timestamp, is unsafe without explicit domain mapping. Reject ambiguous matches and preserve decimal scale, null distinction and external IDs. Automatic mapping must be confirmed by a replay oracle, not a convenient column name. |
| 3167-3332 | Completion and reference grouping | Later-key observation proves closure only for an ordered, completeness-guaranteed stream; late events require another contract. Per-stream queue counts do not cap total bytes, and reference-plus-index groups pin entire parent batches until release. Completion materialization can hold originals plus take/concat outputs concurrently. Fail, wait, spill and approximation are separate policies; waiting on a full stream may deadlock without progress/reclaim rules. |
| 3333-3483 | Hot state, validity and compaction | EntityCache bounds and one-epoch scope do not prove arbitrary algorithm state fits; a giant epoch can exceed the machine. Versioned rows and copy/switch retention are established storage patterns. Old/new compaction overlap and retained checkpoints must fit the prepared cap. The source-keyed example labeled pagerank_pull lacks a demonstrated incoming-access path; access-pattern claims require direction-specific validation. |
| 3484-3594 | Retry, pending writes and conditional reads | Queue-age/weight controls expose freshness versus throughput, but pending data is not durable and every retained request needs a byte bound. A current PageRank property overlay is not proof of a globally converged score vector for the latest graph. Sealed-only and conditional source visibility need separate semantics and customer acceptance. Deletion inference, source bind limits and fallback physical scans are part of lifecycle cost. |

| 3595-3702 | Epoch commit and recovery fixtures | State updates, source runtime metadata, errors and cursor advancement must commit atomically; asynchronously spawned compaction needs its own admission. Pending-revert, poison/restart and import-floor tests are good operational contracts. A common runtime is useful only insofar as it supports the selected customer workflow; building a broad control platform before proving demand remains a prioritization risk. |
| 3703-3844 | Registry, notifications and source liveness | Durable command/index/compaction status aids restart and diagnosis, but metadata records alone cannot bound RAM as the prose claims. Pointer notifications move large payloads to retained artifacts rather than eliminating their bytes. Periodic source polling prevents missed-event stalls but adds recurring source and worker cost. Error retention needs privacy and storage limits. |
| 3845-4006 | Dynamic sources and hidden physical state | Runtime-discovered sources require transactional addenda and stable template identity across copies. Discovery may grow without bound and must trigger re-admission. Copies must preserve or rebuild hidden indexes, sketches and jump tables, with rebuild cost paid. changed_since emits relevant operations but is not necessarily a cheap physical delta scan or a correct incremental graph algorithm. |
| 4007-4152 | Four recovery outcomes and storage activation | Transient clear, deterministic rewind, mismatched recovery and unsafe-frontier refusal are distinct. Source operation replay does not universally beat snapshots: deletions can force broad component repair, and PageRank can have globally propagating effects. Copy/verify/activate with a retained rollback source is operationally sound but may violate the prepared 50-GB cap unless overlap is reserved. |
| 4153-4240 | Bounded lineage and recurring reuse | Accepting missing ancestors outside a checked window is partial lineage assurance, not complete provenance. Additive current-bucket math does not prove incremental PageRank, connectivity or shortest paths. The claimed stable recurring shape still permits variable queries and source-specific distance artifacts may have low reuse. Ask refresh interval, queries before expiry, acceptable staleness and actual source-retirement cost before choosing this architecture. |

| 4355-4447 | Verification analogies and orthogonal metrics | Security tooling, OKF and temporal-memory papers supply design analogies, not a Neo4j buyer. Reviewed definitions can have failed runs and successful attestations can reference stale definitions. End-to-end time and actual planner path matter alongside RAM. The table muddles spill bytes with resident state: disk spill, mapped virtual bytes and physical page residency must remain distinct quantities. |
| 4448-4528 | Next-action usefulness and weak metrics | Reruns, broadened slices, fallback and abandonment are useful customer-behavior measures; moving on does not prove correctness. Output-citation allocation ratios undercount state needed to rule candidates out. Lost raw logs correctly weaken claims to indicative estimates. Use these measures to test first useful answer and repeat adoption, not to label unused graph pages waste automatically. |
| 4529-4674 | Mode UX and daemon economics | Local/cloud/privacy/latency distinctions can reduce switching friction, but six modes and a daemon impose operational cost. Whole-machine accounting includes the parent, warm registry and retries; OOM-driven shrinking is not preemptive hard-budget admission. Contrary to the prose, WCC can consume an exact full edge scan with suitable vertex state, and a local lookup can scan badly without an index. Component/community-based partitioning also has its own discovery cost. |
| 4675-4861 | Structured harness and finite menus | The prose explicitly recognizes the graph-harness paper as theoretical. Finite actions constrain an agent interface, not the supported variable query space. Harness-G's many matrices, ID maps, lexical indexes and optional embeddings require total build/reopen accounting. Artifact validation is useful but does not itself establish low RAM, correctness of extracted meaning or willing buyers. |
| 4862-5018 | Planner counterfactuals and verifier states | Same-state alternative experiments need their own resource budget and cannot relabel predicted alternatives as measured. Checksums and sampled parity prove only scoped properties. StructAgent's unavailable auditor does not refute completion, so it is not evidence for an unconditional final-audit barrier. Confirmed/refuted/inconclusive/unavailable distinctions should apply to memory instrumentation and oracles as well as answers. |
| 5019-5104 | Post-run learning boundaries | Frozen within-run configuration and separately gated repairs protect replayability. Learned heuristics can overfit one dataset, retain private traces or grow without bound. Living-Harness limitations explicitly leave stale removal, rollback and regression work unresolved; a proposed profile-learning layer is not a solved or necessary first product feature. |

| 5105-5206 | Modeled execution and granularity | Coarse/fine storage units trade planning and bookkeeping against selective access. A raw fallback is not inherently the most expensive or nondeterministic path; a generic engine can also admit and spill. A modeled workload family need not freeze query parameters. Harness token-reduction claims are vendor/research claims, not measured graph RAM savings. |
| 5207-5354 | Trace, memory and compaction | Data and execution graphs are distinct. Privacy and recurrence gate trace collection, while calling context RAM is a metaphor, not physical accounting. Unbounded agent memory is asserted without buyer graph sizes or query frequency. Local PPR does not remove global normalization or all vertex state automatically; changed-neighborhood label propagation is not Leiden parity. Memory compaction can change answer quality. |
| 5355-5502 | Ecosystem and task evidence | Extraction, storage, retrieval and evolution all contribute lifecycle cost. Survey authority and quadratic retrieval costs do not validate buyers. Memory-task recall, generation, judge quality and hardware are separate metrics. A watchlist is not paid adoption; three simultaneous integration tracks could fragment early product effort. |
| 5503-5650 | Quality modes and receipt limits | The proposed 1-GB exact-ish, 2-GB broader and 5-GB high-confidence modes lack a quality definition; 5 GB also exceeds this task's cap. A naive full-graph baseline is weak. taOS is explicitly not a Neo4j benchmark or graph engine. Hash/span receipts attest to representation, not truth; redaction preserves only the redacted representation. Optional verification must not disable the hard physical cap. Raw archives plus derived data and repair backlogs need storage admission. |
| 5651-5791 | Memvid packaging and recovery | Embedded WAL, frames, indexes, footer and optional crypto are established packaging ideas, not proof of graph RAM savings. A copied read-only fixture is useful. Recovery backscans, parallel segments, encryption temporaries and full copies are paid lifecycle work. The last valid footer establishes a recovery boundary, not semantic correctness. The prose itself rejects one file for every production role; illustrative 1/5/20-GB modes are not the current four-billion-byte contract. |
| 5792-5854 | Omnigraph authority and derived layouts | Logical authority separated from rebuildable acceleration and branch snapshots are prior art. Exact fallback is valid only when it fits; otherwise return a typed refusal. Version identity must key results and caches. Retained branches and old generations consume the prepared cap, while adding a second engine creates integration and operational friction. |

| 5855-6014 | Backend costs and physical-mode selection | The archive explicitly denies that Omnigraph proves Knight Bus RAM savings. Latency decomposition, cold/warm freshness and separate operation/verification phases improve measurement; they do not excuse omitting setup from end-to-end cost. A finite slope sweep is evidence within tested sizes, not a universal bound. A large-frontier CSR fallback still needs admission, including construction and resident page cache. |
| 6015-6074 | Negative design gates | Blocked/rejected designs and backend-specific skip/fail states are valuable counterevidence. Capture-once authority and delta discovery require conflict, insert, update and delete rules. Retention pins must precede publication and survive rollback, but retained old/new artifacts count toward prepared storage. Hashes outside timing verify identity, not floating-point correctness or valid parent equivalence. |
| 6075-6208 | Product scope and output admission | Direct maintenance, served execution and local aliases are different authority planes. The strongest switching requirement is that rebuilds be explicit and exports byte-bounded; a compact format alone is insufficient. Per-actor request/byte limits still require a global combined limit. The 5-GB/12-GB example is hypothetical and not admissible on the specified machine. |
| 6209-6289 | Local/served parity and scope discipline | One profile behaving consistently across transports is narrower than Neo4j compatibility. Scrubbing volatile fields cannot justify exact JSON comparisons for intentionally noncanonical outputs or differing physical receipts. The prose itself says the full control plane is overkill; keep minimum admission, schema and cancellation contracts without making registry/cluster features a prerequisite for testing a buyer's workflow. |
| 6661-6758 | GraphBLAS/LAGraph semantics | Cached transpose/degrees have explicit build, reuse and invalidation costs. GAP and sink-redistributing PageRank are different contracts; calling one production-correct does not make it Neo4j-compatible. The illustrative 5-GB, three-pass profile neither satisfies this cap nor proves convergence at the stated tolerance. Push-only versus push/pull trades prerequisite storage against work, not answer semantics. |
| 6759-6874 | Output oracles and prioritization | BFS parent invariants, component equivalence, distance sentinels and exact integer triangle counts require different checkers. Edge-consistent component labels alone can falsely merge disconnected components; an independent oracle must catch that. Weighted SSSP and triangle layouts add state. The proposed PageRank-first order is engineering tractability, not ranked customer value. A LAGraph reference is useful where semantics align, not mandatory proof for every profile. |
| 6875-7053 | GBBS and residual-state counterclaim | Sparse/dense frontiers, blocked edge maps, packed state and output elision are established implementation techniques. Residual PageRank carries extra Delta/nghSum/frontier arrays and need not use less RAM or converge faster. Preflight degree/supernode/weighted analysis and oracle work must themselves be budgeted. Correctness reference, implementation comparison and customer product baseline are distinct; the archive explicitly says GBBS is not tiny-RAM product proof. |
| 7054-7160 | Partition economics | CUTTANA/Triparts/TuskFlow are source-reported analogues, not local results. Partition quality must repay buffering, assignment maps, refinement, verification restream and refresh costs before the snapshot expires. Edge cut, triangle locality and p99 are different objectives; a broad or wrong pattern can harm performance. Ask actual queries per freshness interval before investing in a layout, and retain a no-layout baseline. |

| 7161-7302 | Query-aware placement and pass budgets | Workload motifs, filters, source distribution and query frequency are legitimate placement inputs, not globally fixed query requirements. Query rewrites require semantic equivalence beyond a hash. A one-pass partitioner can still own O(V) assignments, replicas and model state; a ladder is not a monotonic RAM guarantee. The hypothetical customer willing to wait at 5/10 GB is not observed demand at four billion bytes. Charge all restreams and stop at the measured quality/reuse break-even. |
| 7303-7444 | GNN expansion versus product proof | High-dimensional features, gradients and sampled neighborhoods change the dominant state and proof type. Artifact rows expose shared CSR, pending samples, overlap matrices, GPU reserves and host copies; a per-GPU cache cap omits them. These are source-reported bottlenecks, not PMF for a Neo4j replacement. The text alternates reserve schema now versus add fields later; defer speculative integration until an actual workflow requires it. |
| 7445-7514 | Marius simultaneous ownership | Current partition buffers, lookahead prefetch, async writeback, global/local maps and rebuilt subgraphs can coexist. Batch-drain waits are correctness and liveness boundaries, not free overlap. Effective derived settings matter more than user knobs alone. The archive expressly disclaims a Neo4j/PageRank benchmark; borrowing choreography does not establish a four-billion-byte end-to-end build or query. |
| 7515-7680 | Distributed/GPU proof boundaries | CDFGNN's master/mirror metadata, quantization side data and accuracy/convergence are unlike exact graph output. Hyperion requires GPU/NVMe/controller and mapped-host infrastructure, so cheap SSD capacity alone does not establish cheap deployment. Count all devices, queues, IPC, pinned topology and source service. Per-miss logging itself needs aggregation/retention limits; a useful receipt need not persist private row addresses. |
| 7681-7761 | Ginex foresight costs | Future-trace caching pays three planning passes, node-sized address/frequency tables, next-access arrays, candidate state and saved changesets. Window integer limits, alignment padding and sampling replacement policies matter. A known algorithm family does not grant a known future access trace for arbitrary queries or adaptive traversals. The cited 6-GB caches exceed the present cap; cgroup/read-ahead/swap posture is evidence context, not whole-machine proof. |
| 7762-7856 | GIDS and centrality cache hints | The reported hidden COO/CSR conversion is a concrete build-phase risk, but its 700-GB and 392x claims remain archive-reported. A 4-GB cache plus CPU backing, queues and GPU state is not a 4-billion-byte machine. PageRank-derived admission is prior art and only a reuse proxy; compare degree/frequency/simple cache policies including ranking cost and refresh. Preserve heterogeneous ID offsets and account for merged return buffers. |
| 7857-7900 | Zebra's bounded-neighbor ambiguity | Top-k temporal influence bounds each retained neighborhood, not the number of nodes, configurations, norm arrays, dictionary overhead or validation/test backups. It changes the representation-learning input and is not exact full PageRank or general temporal reachability. Its usefulness must be judged with task quality and update cost, not presented as semantics-preserving graph pruning. |

| 7901-7947 | Zebra evaluation boundaries | Streaming/pruning strategy, timestamps, alpha/beta ensembles and train/validation/test resets define the workload. Backup and clone costs belong in peak accounting. A claimed avoided recursive frontier is an estimate unless actually measured. Approximation guarantees need their original assumptions and repeated task-quality checks; the text explicitly excludes Zebra from exact Neo4j/GDS parity. |
| 7948-8057 | DUCATI allocation hypotheses | Benefit-density sorting across adjacency/features is concrete prior art for multi-state budgeting. OOM-terminated slope probes are not safe admission under this hard cap, and probe/reorder/CSC rebuild costs must be included. Greedy density is not an optimal indivisible allocation proof; mandatory correctness state cannot compete like optional cache rows. Sampled benefit curves need holdout workloads and post-run error checks. |
| 8058-8155 | PaGraph baseline and replication | Top-degree caching is a useful cheap comparator; the first training step's peak is not necessarily the maximum later peak. Row-capacity calculations omit map/flag state unless added explicitly. Hop-expanded partitions replicate feature and ID state. The archive itself flags suspicious neighbor-order code, so neither copying implementation nor treating its hit-rate bound as universal is justified. |
| 8157-8259 | GNNLab plane accounting | Dedicated sampler/trainer GPUs reduce contention by adding resources, not proving lower combined deployment cost. Frequency tables, ranking sorts, resets, remaps and workspace freelists are live state. Offline/fake-optimal files are hindsight comparators, not attainable general-query predictions. Old CUDA/DGL/PyTorch assumptions add reproducibility and switching work; the archive expressly denies exact OLAP speed proof. |
| 8260-8382 | Compression contract | EXACT is approximate activation compression despite its name, and QGTC is GPU quantized learning. Exact-decode adjacency is already established, including GDS in earlier rows. Count metadata, padded tiles, reconstruction peaks and dense staging, not just final packed bytes. Binary-searching an OOM frontier characterizes tested inputs, not admission for all graphs. Community active-label layouts do not alone implement Leiden/Louvain semantics. |
| 8383-8593 | GraphBolt storage-mode fidelity | Async stage counts, fallback paths and explicit read-into-memory are useful lifecycle contracts; that escape hatch still needs re-admission. Disk-backed features do not imply disk-backed topology. Feature equality is narrower than algorithm equivalence. Observed top-frequency caching is a static-policy comparator, not a universal dynamic-cache upper bound. The claim that sampling is only useful with approximation is too broad: exact CC sampling remains a counterexample. |
| 8594-8650 | Native I/O detail | Direct-I/O header offsets, alignment, partial reads, queues, workers and useful-output copies expose real amplification. Queue-overrun protection alone does not bound aggregate worker/output buffers or prepared artifacts. Detailed path observability supports audit but has not been shown to be a buyer's differentiating purchase criterion. |

| 8651-8774 | Native routing and temporary views | Key partitions, hit/miss positions, output reorder arrays, cache rehash and permutation files all add ownership beyond payload. Narrow IDs require range and remapping proofs; rank-vector width changes require numeric analysis, not only an exact-layout label. Public native controls show inspectable implementation, not proof customers should configure every knob. Temporary count/scan/allocate/copy phases can overlap in peak memory. |
| 8775-8906 | GraphStorm build/run separation | Conversion process count multiplies partition memory. Artifact existence, version gates and train/infer success are useful operational checks but weaker than semantic fidelity and whole-build resource compliance. An AWS benchmark/blog is not evidence buyers understand or pay for a named representation, despite that inference in the prose. The build/verify/run boundary is useful without transplanting its cloud control plane. |
| 8907-9059 | Paired pipelines and high-memory preparation | Small and Papers100M examples use different machines, volumes and trainer counts; neither is a four-billion-byte demonstration. A roughly 1-GB chunk and assumed 2-GB worker do not prove maximum row expansion or combined peak. Paired comparisons must include construction/conversion, all retained source workers and cost, not only epochs. The documented comparison workflow is not observed customer adoption. |
| 9060-9237 | Placement, minimum capacity and semantics | Topology/state/compute placement changes copy-stage ordering. A cache can have a minimum safe capacity and hang below it, so shrinking it is not universally valid. LABOR locality changes correlations and does not automatically preserve an exact graph algorithm. Dropping uneven distributed inputs is unacceptable for exact full coverage; zero-delta blocks require future-reactivation and convergence rules. Current 4-billion-byte accounting replaces the historical 5/10-GB examples. |
| 9238-9336 | Promotion tests and iteration scope | Copy-versus-pin pointer semantics, nested fallback, async completion and original-ID compaction are testable switching contracts. One-iteration PageRank equality does not establish full convergence, refresh or long-run memory stability. Promote with end-to-end failure/recovery and numerical semantics as well as these component tests; a large checklist cannot substitute for measured buyer usefulness. |
| 9337-9400 | Graphalytics boundary | The archive correctly distinguishes public benchmark, mathematical reference and Neo4j compatibility. Its six algorithms provide semantic coverage, not proof of the commercially most painful six workloads. CDLP is not Leiden/Louvain; BFS level integers are not GDS traversal paths, and epsilon validation must be pinned to the benchmark's precise rule. WCC equivalence remains different from exact representative IDs. |

| 9401-9480 | Benchmark completeness | Load/prepare/run/validate/retrieve/terminate/archive/report is a useful full lifecycle. Same algorithm family is insufficient when variants differ; pin the actual contract. Peak RSS, mapped bytes and a page-cache estimate are not interchangeable or naively additive. The historical <=10-GB example is not this task's cap. Total first useful answer must include artifact retrieval and required validation. |
| 9481-9632 | ArcadeDB analytical-view counterevidence | CSR, dense maps, typed columns, explicit readiness and OLTP fallback already exist beside a graph surface. GAV may add memory to buy speed, so its existence neither proves low RAM nor a new invention. CDLP must compare original vertex IDs for ties, not dense order. Count-only execution omits requested output, but a genuinely count-only customer workload should be benchmarked as count-only with an independent correctness oracle. |
| 9633-9786 | Samyama integrated versus direct path | Adjacency lists plus flattened outgoing/incoming GraphView and bidirectional ID maps expose a build peak larger than the final CSR. Pure Graphalytics runtime omits projection, parsing, clones, sort and procedure materialization. Cancelled CI is not a failed test nor green proof. Late materialization and sparse columns are existing techniques; materialize all user-required fields, not merely proof-required fields. |
| 9787-9906 | Complex-read opportunity | Issue 296's high-degree KNOWS expansion followed by dedup, joins, aggregation and sort is a specific technical lead. It remains another engine's issue, not a Neo4j buyer interview. Owned neighbor lists, parent maps, cloned records and pending queues suggest testable lifetimes. Preserve relationship uniqueness, intermediate-path predicates and bag multiplicity; reached-set traversal is not a general replacement. |
| 9908-9978 | Family sidecars and reuse | Friend frontiers, post/date postings and forum counts suggest query-family physical layouts, but two-hop summaries and inverted lists can exceed the prepared cap or lose freshness before amortization. Variable parameters remain supported only within declared predicate/property coverage. The text upgrades complex reads to PageRank's importance, contradicting its repeated earlier PageRank-first certainty; treat both as ranked hypotheses pending a customer episode. |
| 9979-10083 | Neo4j already guards pruning | The archive documents regular path-preserving expansion, heap-tracked BFS endpoint pruning and planner eligibility checks. This directly refutes claims that merely switching to BFS, dedup or compact frontiers is a novel universal win. ExpandInto can prefer DFS; length zero, loops, directedness, context predicates and RHS apply need fixtures. Reject or budget richer state when a path/relationship dependency makes endpoint pruning illegal. |
| 10084-10150 | GDS estimation baseline | Projection and algorithm estimates plus validation already exist; estimates alone are not differentiation. PageRank includes degrees/messages/scaling, BFS includes local chunks and copied results, and WCC already has sampled/seeded strategies with primitive state. Profile alternatives remain hypotheses until complete build/run/output peaks and correct outputs are measured under the actual cap. |

| 10151-10305 | Estimate compatibility and shifting first slice | The archive now recommends a narrow WCC/BFS slice plus PageRank comparator, another change from its earlier PageRank-first prescription. Named-catalog estimates and implicit-projection composite estimates differ. Large fictitious dimensions test arithmetic, not physical completion. Adding cap/profile fields inside existing GDS fields risks compatibility; preserve default schema and link separate native admission/measurement details. |
| 10306-10377 | Lifetime algebra and false max | GDS already has additive, max and range trees, so introducing these operators is not a new invention. The example output tree takes max(stream-buffer, write-batch, checksum), but these can coexist and must then be summed. Persistent topology, result backing and async buffers can survive phase transitions; max-of-phases is valid only after proving actual release and counting shared owners once. |
| 10378-10483 | Existing admission and hypothetical routing | GDS already checks projection/run memory and reserves tasks; routing to a different physical plan is the proposed extension. The 4.42-GB/71-minute WCC example is illustrative, not a measurement, and exceeds the entire current machine before reserve. A sudo/waiver cannot be allowed to silently override this task's hard physical contract. Refusal preserves safety but does not deliver the requested answer. |
| 10484-10558 | Estimate, reservation and observation | The pinned archive describes a global default-min setting and user/admin visibility. Estimate-only does not reserve; reservations do not measure unique physical pages or completed output. Live/completed/released states need failure recovery and bounded retention. Per-job policy may be useful, but accurate reconciliation and useful completion must be demonstrated rather than inferred from new receipt rows. |

## Final Coverage Union

- Reader05 direct spans: `31-167; 225-921; 924-1208; 1981-2548; 2695-4240; 4355-6289; 6661-10558` = **9,066**. All read semantically in bounded complete outputs. Prefix inheritance = **1,587**, yielding **10,653/10,653**. No direct reading beyond the boundary.
- [Reader02 ledger](/Users/amuldotexe/Desktop/personal-repos-lane/knight-bus-graph-walker/research_4gb_20260919/02-radar-middle-Evidence.md:3), consumed here in full lines 1-84: **7,360** independent lines, **492** inherited reader06 lines and **24** byte-confirmed duplicates = **7,876/7,876** for 10654-18529. Its 21 exact independent intervals are at ledger line 10; no new source reread is claimed here.
- Reader02 duplicate credit: `15596-15606` from read `13057-13067`; `16347-16353` from read `15018-15024`; `16365-16370` from read `14726-14731`. That reader reports complete byte-span cmp equality. This lane inherits that explicit verification, not an independent cmp run.
- [Reader03 ledger](/Users/amuldotexe/Desktop/personal-repos-lane/knight-bus-graph-walker/research_4gb_20260919/03-radar-tail-Evidence.md:13), consumed here in full lines 1-64 and 65-113: **6,958/6,958** independent lines, contiguous 18530-25487. All 1,828 queue entries were read there; no recursive linked-source reading or duplicate exemption. A five-token elision at 22668 was repaired there with a complete 22666-22670 reread.
- Final disjoint arithmetic: **23,384 independently read + 2,079 inherited reader06 + 24 exact-duplicate credit = 25,487**. Interval-union audit found no gaps or overlaps and endpoints 1/25487. The 492 reader06 lines inside reader02's region are counted once, not added again. The newly inherited 95 lines are included in 2,079, not 1,984.
- Reader05 truncated source displays at 133-167, 785-814, 869-881 and 914-921 were repaired before credit; 971-1208 used the lossless URL-prefix abbreviation above. The later prefix prose was fully displayed, not heading/keyword-scanned. A broad reread of this lane's already-complete product artifact was truncated; only its required primary-check section 68-82 was reread completely and used, with no new source-coverage credit.

**No unread assigned source span remains.** This is a full PMF003 content-coverage union, not independent verification of its hundreds of linked sources, an algorithm benchmark, or proof the parent goal is complete. Original 32-document evidence and additional 19-document/XLSX evidence remain separate and unchanged.

## Product Decisions

**Investigate a budgeted analytical companion before claiming a replacement database.** The useful promise is a supported decision workflow: accept CSV or a consistent Neo4j export, build within a declared envelope, answer variable queries in supported families, deliver complete useful output and refresh before the answer becomes stale. The archive is rich implementation research but does not supply a paid buyer, replayable customer job or measured four-billion-byte lifecycle. Repeated author confidence, paper speedups, example caps and profile names do not strengthen demand evidence.

The original product and transferred PRD04 ledgers retain the broader buyer assessment. Product judgment here prioritizes consequential blocked decisions, costly current behavior, alternative solutions and the experiment with the highest information value. It is inspired by Shreyas Doshi's product-judgment lens, without impersonation or attributed quotations.

| Rank | Workload opportunity and buyer hypothesis | Evidence strength and switching friction | Decisive rejection condition |
| --- | --- | --- | --- |
| 1 | Security/dependency/inventory neighborhood investigation, sometimes followed by joins, counts or ranked findings; platform/security engineer operating it, team lead owning budget | Narrow existing Bolt proof supports some endpoint reads; archive 9787-10083 identifies complex-read costs and existing Neo4j pruning. No customer demand proof. Need typed/temporal predicates, original IDs, required witnesses, driver/error behavior and downstream joins. | A query/index/scoped-rebuild fix removes the pain; required path/bag semantics defeat specialization; fresh source reads are mandatory and snapshot age is unacceptable. |
| 2 | Recurring connectivity/component-drift reports; data/security operator and analytics/platform budget owner | WCC has strong semantic fixtures, not proven customer value or bounded ingestion. Isolates, propertyless edges, seed IDs, deletions and complete output are essential. | Membership does not change a decision; refresh cannot finish before expiry; random external parent probes erase useful latency. |
| 3 | Scheduled ranking/PPR for investigation priority; analyst/data scientist and analytics platform lead | PageRank has rich state/convergence evidence; source repeatedly chooses it for familiarity, not observed purchases. Must pin weights, sinks, personalization, iterations and result scaling. | Degree/frequency ranking is equally useful; dense scans exceed the deadline; preparation and score export dominate the number of useful runs. |
| 4 | Filtered similarity/co-occurrence candidate reports; fraud/recommendation engineer and domain product owner | Middle/tail ledgers show concrete topK/topN, filter and zero-score counterexamples. Exact comparison and sampled GDS KNN are distinct contracts. | Exactness adds no decision value, candidate work remains near-quadratic, output exceeds retention budget, or a candidate update invalidates most results. |
| 5 | Weighted exposure/route analysis; domain analyst and platform owner | Route carriers, negative-cycle modes and bucket state are well documented. No buyer evidence; weights, ties and requested route payload create migration burden. | Workload actually needs unrestricted path enumeration/all-pairs payload, unsupported weights or little reuse before refresh. |
| 6 | Community and motif reporting; graph analyst/research lead | Middle ledger adds substantive Louvain/Leiden refinement and contraction state, not measured RAM wins. Heuristic outputs and triangle integers need different quality/correctness gates. | Rarely acted-on exploratory output, unstable quality, nonshrinking generations or rebuild time exceeds useful cadence. |
| 7 | Graph embeddings/training preparation; ML engineer and ML platform lead | Strong external-system analogies, weak Neo4j replacement evidence. Dense vectors, optimizer, feature extraction, model registry and GPU/host cost dominate switching. | Model/retained output alone exceeds budget, quality falls, or savings merely move cost to uncounted host/GPU/storage tiers. |

Ranks order experiments, not removal of lower-ranked families from the goal. An embedded analytics worker sold through an existing graph-product vendor remains a channel hypothesis across these ranks; original practitioner interest is not a workload or buying commitment. Do not sell receipts instead of a useful answer.

### Lifecycle Requirements And Counterclaims

| ID | Requirement to verify | Counterclaim or failure to expose |
| --- | --- | --- |
| RA-R1 | Acquire consistent node/edge/property streams and preserve isolates, multiedges, signed/string IDs, nulls, types, weights and temporal meaning; bound CSV field/record decoding. | Counts and property streams are not complete topology; separate exports can mix source versions. Extraction loads the retained Neo4j service and needs its own time/cost. |
| RA-R2 | Enforce `OS/other services + unique simultaneous resident allocations/pages of source(if local), builder, workers, verifier and exporters <= 4,000,000,000`. | Heap, RSS, file residency and mapped virtual length overlap; neither one worker's cgroup nor final packed size proves the whole machine fits. Probes, sorting, queues and recovery also run under the cap. |
| RA-R3 | Prepared persistence totals topology, ID maps, properties, selected indexes, required replay state, retained results/history and pinned generations within 50,000,000,000 bytes. Report scratch and source-volume requirements separately. | A 50-GB prepared artifact leaves no refresh headroom if the entire volume is also 50 GB. External raw inputs need not be prepared state, but retained product replay copies are not free. |
| RA-R4 | Price parse/normalization, external sort, degree probes, index/layout build, validation, cold open and complete output before claiming time to first useful answer. | One-row BFS may be a giant nested list; topK per source is not a global constant; first row is not completion. Build-only and query-only measurements are attribution views, not total workflow. |
| RA-R5 | Preserve legal path/reached-set/bag semantics, predicate timing, weights, tie rules, original-ID randomness and convergence; fail explicitly when unsupported. | Early dedup/filtering or changed sampling may silently change the answer. Exact decode, exact algorithm semantics and exact bitwise floating output are different promises. |
| RA-R6 | Reserve build/run/export concurrency and all retained owners; release on normal completion, cancellation and failure, with recoverable publication. | Sum simultaneous owners, max only nonoverlapping phases. TTL is not a byte cap; slow readers and exporters can pin more than two generations. A resource-admitted job is not a completed validated job. |
| RA-R7 | Measure change discovery, deletes/property changes, remapping, algorithm-specific repair, verification, activation, rollback and reclamation. | Source delta availability is not O(delta) physical I/O or correct incremental execution. Local unchanged topology does not imply unchanged incoming PageRank contributions. Seeded WCC is not deletion-safe maintenance. |
| RA-R8 | Establish freshness at answer completion, complete-answer deadline, queries per version, reuse by family and variance, and retention requirements with the operator and decision owner. | Snapshot-first is deliberate, not universal; live analytics exist. Delete/revocation deadlines can be stricter than insertion deadlines. A safe refusal or endlessly stale backlog is not useful delivery. |
| RA-R9 | Compare combined retained source + worker + build/refresh + storage/egress + required verification + operational cost over the same customer period. | Do not credit licenses or source machines that remain. Use tuned Neo4j, a temporary larger analytics session and a simpler non-graph answer where appropriate, not an intentionally naive baseline. |

For reuse economics, measure `extra preparation/refresh cost <= useful runs before expiry * measured per-run saving`, with all terms in either time or money, never mixed. This is a conditional break-even test, not a forecast. Ask for the last failed job, how often it recurs, the decision it delayed, acceptable age when the complete answer arrives, number of variable queries before refresh, who operates extraction and which existing bill actually disappears.

### Verification-First Experiments

1. **Customer episode and incumbent repair.** Obtain an authorized replayable input/query/output episode and current source/worker bill. Test existing query-plan/index/scoped-rebuild repairs first (670-675; 9979-10083). Falsify the replacement opportunity when this resolves the consequential pain. No interviews or customer tests were performed in this lane.
2. **Cold-to-refreshed scalar workflow.** Run CSV and consistent Neo4j acquisition through bounded normalization/build, WCC or a precisely pinned PageRank, full original-ID export and one insert/delete/property refresh. Include hub skew, sparse IDs, propertyless edges, isolates, tiny cache, slow output and crash/activation faults. Falsify on any cap violation, missing output, semantic mismatch, missed deadline or combined-cost loss. A small oracle run verifies meaning; a separate scale run verifies actual feasibility.
3. **Eligibility-checked complex read.** Compare legal compact endpoint/frontier-to-aggregation plans with Neo4j's legal pruning plan, retaining full requested filters/joins/sort/output. Use diamonds, cycles, length-zero/min-depth, intermediate predicates and parallel relationships to catch illegal path-to-set replacement. Include sidecar build, invalidation and variable parameter replay. Falsify when the real query needs richer semantics or cold/refresh cost erases repeat gains.
4. **Reuse/freshness crossover.** Sweep one-shot through repeated-query workloads and refresh frequency, including changed query distributions. Compare no extra layout, simple degree/frequency cache, sequential blocked access and a budget-selected layout portfolio. Count probe/sort/reorder/ID-map/current-next-buffer costs. Falsify the more complex planner if it does not repay preparation before expiry or shifts the peak into refresh.
5. **Output/state floor test.** Before similarity, community or embedding investment, calculate required output/state bytes at the customer's actual counts and widths, then test tiny exact/quality oracles and a bounded scale path. Include zero-score similarity, asymmetric filters, nonshrinking contracted graphs, full route payload and dense embedding export. Reject impossible retention contracts explicitly; do not relabel truncation, stats-only or lossy output as successful parity.

### Prior Art And Implementation Claims

The prefix and both downstream ledgers already contain physical-profile routing, shared primitive state, external typed results, source-owned similarity queues, walk regeneration proposals and spillable community generations. Therefore the earlier 05-product H1-H4 hypotheses must not be promoted as globally new merely because they combine these names. The remaining contribution to establish is a specific semantic proof plus a measured build/query/refresh cost improvement within the customer's full envelope. No such result was produced by reading this archive.

Reuse the primary checks already performed in the original lane on 2026-09-19, not a new sweep: [GDS feature toggles](https://neo4j.com/docs/graph-data-science/current/production-deployment/feature-toggles/) document CSR with default compressed adjacency and optional packed off-heap storage; [memory estimation](https://neo4j.com/docs/graph-data-science/current/common-usage/memory-estimation/) documents prechecks without guaranteeing no exhaustion. This independently rejects the pointer-object-everywhere premise. [GraphChi](https://www.usenix.org/conference/osdi12/technical-sessions/presentation/kyrola), [GridGraph](https://www.usenix.org/conference/atc15/technical-session/presentation/zhu) and [Load the Edges You Need](https://www.usenix.org/conference/atc16/technical-sessions/presentation/vora) are prior art for external graph execution, blocked layouts and change-aware I/O. Pin versions in any experiment; neither these primary checks nor archive source summaries prove broad novelty absence or present feasibility.

Existing implementation evidence remains the narrow paired warm Bolt workload in the original product ledger, not a full GDS or bounded-import/refresh implementation. The PMF003 registry/fixture narrative repeatedly calls algorithms registered-but-unsupported and later corrects missing-literal searches using the TSV. Source-reported test counts, golden JVM bytes and future acceptance tables must stay separated from current executable support. This lane ran no algorithm code and modified no implementation.
