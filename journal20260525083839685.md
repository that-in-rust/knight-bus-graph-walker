# Journal - 2026-05-25 08:38:39 UTC

## Work Prep

1. Created branch `neo4j-replacement-01` from `main`.
2. Shallow-cloned the Neo4j reference repo:
   ```
   git clone --depth 1 https://github.com/neo4j/neo4j.git neo4j-reference/neo4j
   ```
3. Added `neo4j-reference/` to `.gitignore` — the clone is kept local for
   reference only and is not tracked in version control.
4. Created this journal file to track objectives and progress.

## Objective

To create a Rust replacement of neo4j with 2 variants:

- **variant_low_RAM**
- **variant_low_latency**
