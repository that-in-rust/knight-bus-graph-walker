# PMF003 Radar Middle Evidence

Status: assigned range complete. Assigned only `docs_PMF_01/PMF003-Graph-Developer-Alpha-Radar.md` lines10654-18529 inclusive (7,876 lines):7,360 independently read,492 explicitly inherited,24 exact-duplicate credit. No linked-file recursion, source/code edits, implementation, algorithm experiments, agents or commits. Prior completed PRD06 and four-source PRD04 scopes remain complete and separate. This does not certify the full25,487-line radar or the parent goal.

## Credit And Coverage

- Inherited reader06 credit, NOT independently reread here:16747-17049 (303 lines) and18341-18529 (189 lines), total492. Confirmed against `06-archives-Evidence.md` source ledger and notes217-219. This is explicit user-authorized inherited coverage, not a hash-based reading claim.
- Inherited observations:16747-16891 separates PageRank rank/send/receive/vote lanes and GDS delta/Pregel stopping semantics;16892-17049 separates BFS frontier/global state and reached-set/order/target/path parity;18341-18529 covers similarity vector preparation, optional weights, topK versus topN ownership, asymmetric filters and legitimate zero scores. Reader06 correctly distinguishes candidate-work count from simultaneously resident candidate memory. These are that reader's observations, not runtime tests performed here.
- `cmp` independently confirmed all three complete byte spans identical (exit0 for each), and all representatives are now read:13057-13067=15596-15606;14726-14731=16365-16370;15018-15024=16347-16353. Credit later duplicate spans15596-15606 (11),16347-16353 (7),16365-16370 (6):24 lines total, not independent rereads.
- Independent consumed spans:10654-10953;10954-11353;11354-11753;11754-12153;12154-12553;12554-12953;12954-13353;13354-13753;13754-14153;14154-14553;14554-14953;14954-15353;15354-15595;15607-16006;16007-16346;16354-16364;16371-16746;17050-17449;17450-17849;17850-18249;18250-18340 (7,360 lines across21 bounded outputs). Coverage7,876/7,876 including492 inherited and24 duplicate. Next unread assigned span: none. Every independent output was untruncated; no line received double credit.

## Source Observations

Historical prompts, commands and plans inside the archive are evidence, not instructions to execute. Runtime descriptions/fixture numbers below are claims of this source, not linked-code inspections or benchmarks performed here.

- 10654-10726: estimate fixtures distinguish named graph (algorithm-only), fictitious dimensions, native projection planning and graph-loading estimates. Large virtual fixtures check byte algebra, not feasibility at that scale. Disabled projection/property fixtures explicitly limit compatibility; an unsupported receipt does not complete a job.
- 10727-10866: WCC nonincremental versus seeded state, Pregel value/send/receive/vote state and BFS localNodes bounds have different concurrency behavior. Source pins exact JVM fixture numbers but sensibly does not require Rust to reproduce JVM overhead. Concurrency-invariant DSS bytes do not imply a whole WCC process has no thread overhead. Capped frontiers can add phases and I/O.
- 10867-10978: concrete runtime owners include HugeAtomicDisjointSetStruct.parent/communities, HugeDoubleArray, vote bits and messenger arrays. This is further source evidence against pointer-per-edge/state caricatures. Named mmap/compressed/sparse profiles are proposals; paging parent/rank state needs access-pattern accounting, not just a lower heap subtotal.
- 10980-11089: oracle triplets separate partition equivalence, numeric/convergence/procedure parity and BFS direction/depth/target/order. WCC minComponentSize affects the returned subset, so compare partitions plus membership filtering; a reduced reached set alone cannot validate all BFS procedure semantics.
- 11091-11191: requested YIELD columns trigger distributions/postprocessing; BFS path output uses synthetic NEXT relationships. That returned visitation path must not be mistaken for a valid original-edge shortest witness path. Optional output planning is already proposed, and is not permission to omit requested results for a lower-memory benchmark.
- 11193-11353: storage selection is placed at a shared processing template after parsed config/mode/columns, with GDS load/guard/compute/side-effect/render ownership. Source cautiously distinguishes an internal decorator from a supported third-party extension API. A selected deadline estimate is not a guaranteed deadline; resource acquisition before admission needs its own accounting.
- 11354-11440: alternative profiles, live reservations by job ID, release and measured reconciliation are explicit prior architecture. Example8.7GB/4.2GB plans are illustrative and neither fits the present4GB whole machine. Endpoint-only/path-on-demand options preserve exactness only for the actually requested output, not all procedure shapes.
- 11442-11573: stream/stats/mutate/write are separate phase budgets, including retained graph-store properties and result store. Compute fit does not imply mutate/write fit. Peak memory follows live overlap, not blindly summing disjoint phases or forgetting results retained into later queries.
- 11575-11705: native writeback mutates mapped database IDs while deferred result-store entries retain vectors and mappings; expire-after-access is not a capacity bound because repeated access can keep entries alive. A cancellation fixture starting already-terminated does not prove rollback after partial batch commits. External typed results and resumable materialization are prior proposals; mmap is not a hard physical-RAM guarantee.
- 11706-11753 begins relationship exporter ownership: degree-partitioned native writes versus sequential input with two write and two pool buffers. Double-buffer overlap and source graph lifetime must be counted; this subsection continues in the next span.
- 11754-11876: four deferred relationship carriers retain topology, one-property graph, one-shot stream or composite iterator plus ID mapping. Typed edge bundles and explicit stream lifetime are already developed, not just a format name. An unconsumed iterator can pin the entire original graph; externalization must sever or budget that ownership.
- 11877-11991: path writeback can hide node/cost arrays in each emitted edge; K-spanning instead returns node properties. Tree output n-1 only holds for its declared effective connected tree, not arbitrary forests (n-components), empty inputs or disconnected support modes. Correct carrier selection is useful prior planning, not evidence a kernel fits.
- 11992-12149: BFS produces one row containing the entire traversal list and optional virtual Path, while WCC/PageRank expose scalar vectors. One-row output is not bounded payload; a generic streamed-row solution must handle a huge nested value without silently changing this API. Carrier versus target separates logical result from persistence/residency.
- 12151-12312: similarity bundles carry root/original maps, orientation, filters, topK/topN, distributions and convergence metadata. The n(n-1)/2 pair formula is not a universal bound for directed per-source KNN outputs, which may retain both directions; topK-ish is not an admission proof. Exact all-pair work, output count and live queues are separate. A capped bundle is exact only if it spills all required rows or obeys the requested limit, not silent truncation.
- 12314-12486: variable-length route bundles explicitly separate fixed endpoints/costs, node/relationship IDs, offsets, costs and negative-cycle mode. Source explains eager result-store collection as preserving data past transaction termination; lazy replay therefore needs detached immutable data, not merely a delayed cursor. An exact estimate of total route payload is not generally available before graph work; bounded incremental spooling plus disk admission/refusal semantics must be specified.
- 12488-12680: compact f32/bitset embedding carriers may expand into dense/boxed API values. Matrix artifacts and bounded row materialization are already proposed. KGE has source/target bitsets, model/scorer inputs and existing-link exclusion, so a shared score bundle does not make its compute equivalent to KNN. Output serialization and model/training provenance count.
- 12682-12883: a substantive correction to output-only embedding accounting: FastRP output+A/B work matrices, Node2Vec walks/probability cache/center+context matrices, GraphSAGE inference versus training/optimizer/sampled batches, and HashGNN compact caches plus dense materialization. These are better baselines for H4 than the atlas. Fixtures are source-reported arithmetic; the absent searched KGE estimator is an explicit search caveat, not proof it never exists.
- 12885-13159: repeats starter estimate/owner/oracle proposals with concrete public schema and optional profile identity. Rust need not match JVM allocation bytes to be mathematically correct; byte-fixture parity should mean an estimator reproduces its specified baseline, then independently estimates the new layout. Profile time/GB examples are illustrative and cannot become4GB completion claims.13057-13067 is the now-read representative of exact duplicate15596-15606.
- 13160-13353: KNN carries per-node lists, four old/new/reverse temporary arrays and sampler-specific scratch; NodeSimilarity adds vector/optional weights/component preparation and distinct per-source topK/global topN. Source explicitly exposes steep topK-state growth. Changing the KNN sampler or sampling cap can change approximation quality and deterministic output, not merely memory/latency. Filter estimator reuse does not excuse omitting newly materialized filter state.
- 13354-13691: tiny similarity fixtures pin seeds/concurrency, bidirectional KNN rows, topK/topN effects, zero-score NodeSimilarity rows, parallel-edge semantics and asymmetric filtered counters. These are valuable counterexamples to positive-overlap-only exact candidate generation. Accepted randomness does not make a tiny fixed-seed fixture a recall guarantee. The proposed omission of requested distribution fields with a cap reason (13687) is a different contract or explicit failure, not successful API parity.
- 13692-13844: KNN sharded delta segments are an actual prior mechanism: checked-state/iteration records, old/new/reverse cursor proposals, per-node synchronized insertion and updateCount/convergence effects. Releasing an unused filtered producer can reduce retained bytes, but preflight selection from seedingSummary is available only once the required seeding work has occurred. It is not necessarily a before-compute choice.
- 13846-14032: NodeSimilarity pushes degree/source/target participation before vector build; required vectors belong to the union of actual source and target participants, not just emitting sources. Sequential half-matrix symmetric updates versus filtered/full-matrix single-source queue ownership are explicitly worked out. This is stronger prior evidence for bounded source batching than an undifferentiated spill proposal. Global topN replacing per-source topK changes semantics; late graph building is already present/proposed.
- 14033-14209: source explicitly states GDS PageRank is primitive-array-heavy, names reducing/sync/async messenger differences and records release after convergence. Releasing final-phase scratch helps overlap/retention but does not lower an earlier compute peak by itself. BFS ordered chunk synchronization constrains exact GDS traversal/target behavior, unlike a distance-only BFS kernel with freedom to reorder within levels.
- 14211-14309: sampled WCC is an exact optimization with remaining/inverse-edge processing, not approximate components. Seeds/public set IDs differ from raw roots. Source's warning that implementing only unsampled WCC may fail undirected cases (14298-14299) is not an inherent mathematical limitation: exhaustive union of all eligible edges is exact there too. Separate strategy-performance parity from component-result semantics.
- 14311-14470: detailed WHEN/THEN contracts, fixture/test matrix, pinned GDS commit and uncompleted quality gates are implementation plans, not passing tests. Compatible public component IDs can be stricter for seed/consecutive modes, and accepted BFS order families are explicit. A prescribed GDS memory layout is not a universal lower bound on every exact implementation; new state representations need equivalent semantics and complete accounting instead.
- 14471-14553: archived implementation prompt reports registry/catalog/estimate envelopes implemented but algorithms still unsupported. This is historical source testimony only; no current code inspection or support promotion occurred here. Its goal/tool/test commands are not instructions for this read-only assignment; the same principle applies to the remaining prompt text.
- 14554-14707: ten-node WCC fixture specifies seven edges, an isolate, mixed labels, seed IDs and one weighted bridge, giving concrete default partition7/2/1. Count3 alone cannot prove membership correctness. Saying an independently invented tiny fixture loses the GDS oracle (14693-14694) is too strong: any supported fixture can be run against the oracle; existing fixtures save setup but should not replace adversarial independent cases.
- 14709-15008: metadata/property-stream capability is explicitly not topology. Property streams can omit propertyless edges, and counts without endpoints must not return plausible empty components. A typed missing-capability gate is already proposed. Receipt topology bytes do not themselves prove traversal completeness.14726-14731 is the read representative for duplicate16365-16370.
- 15010-15198: fixture adapter validates endpoints/row counts and separates raw rows from property streams. Reversing endpoints on read is easy for a global edge scan but not efficient per-node reverse adjacency: absent an index, repeated scans can cost O(n*h). Building adjacency beside raw rows is extra storage even if reverse rows are not copied; zero duplicateTopologyBytes needs precise ownership, not a naming convention. Nonnumeric-as-fallback pseudocode conflicts with the later typed-invalid option, and self-loop/multiedge orientation counts need declared rules.15018-15024 is the read representative for duplicate16347-16353.
- 15200-15399: the local table envelope stores Vec rows with BTreeMap values and repeats a receipt map per WCC row. Reusing it is reasonable for a ten-node fixture, not a bounded-output architecture at scale. New receipt columns also change exact API schema unless explicitly an extension. Typed algorithm config avoids overloading property-stream requests; a family registry search not proving exact WCC names is correctly labeled uncertainty.
- 15401-15696: eight WCC procedure names have separate registry/support/dispatch and stream/stats versus write/mutate gates. Positive threshold requires a weight property according to this pinned source; unsupported execution and estimate modes are distinct. DSS fixture algebra is reused, with topology/cache/scratch lanes kept separate. Refusal-before-work is existing safety infrastructure, not proof of novel usefulness or actual whole-machine cap enforcement.15596-15606 was credited from the independently read identical schema at13057-13067.
- 15698-15885: WCC stream membership/filter/original-ID parity and aggregate distribution/empty-graph parity are separate. Merely checking all expected same-component pairs (15859) without distinct-component separation can accept overmerges. Repeated additive receipts increase output bytes; stats cannot replace requested node rows. Write/mutate require state-transition and invalidation evidence beyond correct computed values.
- 15887-16006: narrows initial runtime to natural unsampled, unseeded WCC and specifies threshold comparison strict > with fallback threshold+1. Restricting strategy is a support choice, not proof unsampled union is wrong on undirected graphs. Floating threshold+1 is not necessarily larger for large finite values or infinity, so edge-case semantics must follow the pinned operator/validation rather than the prose promise that every missing weight passes. Deterministic single-threaded versus concurrent performance remains unmeasured here.
- 16007-16243: important self-correction: absence of WCC literals in Rust did NOT mean no registry entry; the loader consumes a TSV containing all eight names. Preserve this later conclusion over earlier add-registry instructions. Indexed search scope must include generated/data-driven interfaces before declaring absence. No external registry was opened by this lane.
- 16244-16346: another explicit correction: receipts belong out-of-band or opt-in, not added to default GDS stream columns. This resolves the earlier schema critique within the archive itself. The Vec-backed fixture table still needs a separate scale solution. The text returns to topology/property-plane distinction rather than introducing a new algorithm.
- 16354-16364 and16371-16554, with credited duplicate contexts16347-16353 and16365-16370: catalog sample12nodes/34edges but only2endpoint rows is identified as unsuitable for WCC; explicit ten-node/seven-edge fixture resolves that mismatch. Counts without matching topology are not computation. Source again separates one stored row from two undirected logical traversals, without solving indexed reverse-neighbor I/O. Exact duplicate spans were not independently reread here.
- 16555-16642: weighted sibling fixture gives every nonbridge edge10 and bridge4.2; threshold5 yields4components,3.14 yields3. Missing-property and equality cases are distinct. This is concrete semantics evidence, not performance evidence; topology/property joining must retain propertyless edges under the declared fallback.
- 16643-16746: seeded WCC adds communities plus maxCommunityId, negative/missing sentinel treatment, public-ID comparison on union, new-ID assignment and incompatibility with consecutiveIds. It is not just compressed labels or a free refresh hint. Arbitrary existing seed assignments can encode prior equivalences, and deletion-safe incremental component maintenance is not proved by these fixture semantics. The next303 lines16747-17049 are inherited reader06 coverage only.
- 17050-17242: Dijkstra source distinguishes distances, predecessor trees, one/many/all targets, optional relationship IDs and full route payload. Strict-RAM priority/predecessor windows are named, not an external-memory priority-queue correctness/I/O design. Require nonnegative permitted weights for the settled-node algorithm; selected lower payload is a changed result request, not a free equivalent benchmark. Equal costs alone do not prove valid ordered routes, and one chosen path per target is not enumeration of every shortest path.
- 17243-17422: Bellman-Ford has separate frontier membership, atomic distance/predecessor/length ownership (length also locks), local queues and optional cycle vertices. Source suppresses ordinary paths when its reachable negative-cycle detector fires. A source-seeded run does not detect disconnected unreachable negative cycles, despite wording about whether the whole graph is invalid. Detection-only versus explain-cycle output is explicitly separated; queue caps require preserved relax/sync semantics and costed passes.
- 17423-17449 begins Delta-Stepping as weighted topology plus bucketed scratch, not a new durable format; next span completes its estimator and queue design.
- 17450-17598: Delta-Stepping estimates shared-frontier versus per-task dynamic-bin ranges, scans64-node chunks, locally clones/drains small bins, and synchronizes the next smallest bin. Delta is scheduling, not approximation, for its permitted nonnegative-weight contract. Equal shortest distances do not require identical tie paths across delta/schedules unless explicitly standardized. Bounding only the active bin omits future bins and stale duplicates; spilling needs correct bucket closure/order and costed repeated topology/state reads. Shared-frontier allocation by relationship count can exceed an n-based optimistic bound.
- 17600-17782: GDS Random Walk already uses bounded queue, per-task1000-walk staging, timed offers, cancellation on consumer close, source mapping and weighted preprocessing. A new small queue is not a new bounded-stream mechanism and does not bound all outstanding task buffers. Count-visits is a different output from the corpus; maximum corpus bytes still scale with sources*walks*length. Tombstone delivery and cancellation while a queue is full need liveness tests, not just a flag receipt. Seed alone is not a reproducibility contract independent of task order/concurrency.
- 17783-17992: Node2Vec already stores delta/zigzag/vlong-compressed walks and reuses a decode buffer; retainers must copy or respect lifetime. Two-pass probability prepass plus regenerated walks, compressed corpus spill, fused training and paged matrices are explicit prior proposals. Exact regeneration needs identical walk/RNG/source/weight/order contracts, while unchanged marginal frequencies do not guarantee identical SGD updates. Fusion before global sampling probabilities exist changes training unless it reproduces that dependency. Matrix paging can trade corpus RAM for random update I/O; exact-ish is not a usable exactness class. This narrows any generic claim of novelty for replay, compression or embedding streaming.
- 17994-18164: Louvain concretely owns current/next communities, volumes/influences, per-thread high-degree maps, summed-weight contracted CSR and original-node dendrograms. It already retains rolling two arrays without intermediate output; final-only is not a new advantage over a fairly configured GDS baseline. Prior candidate mechanisms include spillable contracted generations, exact spill tallies, deterministic heavy-node chunk/reduce and warm-start deltas. Serializing/merging tallies must preserve the chosen numerical/move-order contract; modularity agreement alone does not prove identical heuristic partitions or skipped-region validity. Old/new graphs, mappings, weighted self-loops and spill merges need live-overlap bytes.
- 18166-18340: Leiden separately enumerates local queues/volume arrays, refinement probabilities/merged volumes/bitsets, aggregation sorting/coordination and post-aggregation maps. This is substantially more developed than merely calling it Louvain. However, naming spill-refinement with deterministic order/RNG does not prove all eligibility/connectivity invariants survive external execution. Connectivity/refinement validation is additional to modularity and labels; warm-start affected-region reuse is unproved here. The final-only output option again overlaps the incumbent's configurable dendrogram behavior. No executed4GB phase or I/O result appears in this assigned section.

## Mechanism And Claim Synthesis

This middle excerpt is an iterative design archive, not7,876 lines of unrelated architectures. Many passes restate the same estimate/owner/oracle/receipt packet; the substantive advances are specific ownership, state transitions, semantic counterexamples and corrections. Tables of profiles and future tests are not completed kernels, bounded-I/O proofs or benchmark results. GDS fixture bytes are pinned source-reported estimator evidence, not this lane's measurements; the referenced GDS commit is `dc4417b3c1feffbdf9eb6293fbbd1ffb1b7233b9` in the archive. No linked GDS/Rust code was recursively read for this assignment.

| Mechanism already present in assigned source | Exact spans | Integration implication |
| --- | --- | --- |
| Runtime-owned arrays plus estimate/phase/mode accounting |10654-10978;11442-11705;14033-14209 | GDS is not pointer objects everywhere and already has admission/state ownership. New claims must identify changed bytes/work under the same output contract. |
| Typed durable vectors, edge scores and variable-length routes with late API materialization |11754-12883 | Outputs, mappings, detached cursor lifetimes and retained writeback are first-class costs. External result artifacts are prior proposals, not free postprocessing. |
| KNN checked-state/iteration delta segments and single producer retention |13692-13844 | Concrete existing sharded state/release mechanisms; exact updateCount, ties and stochastic quality remain constraints. |
| NodeSimilarity participation pushdown and source-owned topK schedules |13846-14032; inherited18341-18529 | Do not rename source batching/queue ownership as new. Zero-score candidates, asymmetric filters, component policy and global topN need separate proofs. |
| WCC fixture topology separate from property streams |14709-15198;16338-16642 | Metadata counts and property rows are insufficient without an explicit complete endpoint contract. Reverse-on-read needs actual I/O accounting. No source entity-attribute factor-native WCC/BFS extension is developed in these independent middle spans; the positive prior witness remains Sol-01 in the completed PRD04 scope. |
| Node2Vec probability prepass and deterministic regeneration candidate |17783-17992 | Replay/training/corpus compression already proposed. A bounded-RAM low-I/O innovation must improve total generation/training traffic at the same quality/semantics. |
| Louvain tally chunking/spill and contracted-generation lifecycle |17994-18164 | Community working state is worked out better here than in PRD06; generic external tally/coarsening and warm-start claims are already prior corpus. |
| Leiden phase-specific refinement/aggregation workspace candidates |18166-18340 | Refinement is not absent from the wider corpus. It is specified as state and future tests, but external-order/eligibility proofs and lifecycle I/O remain to be derived. |

Two self-corrections must travel with any summary:16155-16243 supersedes the implication that missing Rust literals means no WCC registry;16244-16336 supersedes default receipt-per-row schema additions. Earlier claims should not be repeated after their later correction. The final-only Louvain/Leiden and bounded Random Walk proposals also overlap modes the same source attributes to GDS, so a fair comparison must configure the incumbent equivalently.

The main unresolved economic obligation in this excerpt is bounded state AND low total I/O: named spill/mmap/partition variants often omit concrete extra passes, random state probes, build scratch, durable-ID joins, old/new refresh overlap and final output delivery. Runtime/output accounting is substantive, but does not fill those lifecycle gaps by itself. Repeated5GB/10GB example caps cannot stand in for a4,000,000,000-byte whole machine. No new innovation or measured result is claimed by this reading artifact.

## Final Audit

Coverage consists only of the21 independent spans above, two explicitly inherited intervals and three byte-confirmed duplicate intervals. A completed arithmetic check found their sorted disjoint union exactly10654-18529, with7,360+492+24=7,876 lines and no gaps or overlaps. Independent source text was semantically reviewed in bounded untruncated output; arithmetic/duplicate checks validate bookkeeping, not substitute for reading. Artifact diff/explicit trailing-whitespace checks passed. Earlier completed scopes remain unchanged. No assigned unique content remains unread.
